# 2026-09-09 보고서 품질 개선 재개

이전 중단 커밋 `e3d6762d`에서 작업을 재개했다. 포트폴리오 집계는 이번 범위에서 제외하고 기업 규모·산업별 보고서 품질과 하위 작업자 알림 개선을 진행한다. 이전 원본 세션과 트랜스크립트는 변경하지 않았다.

## 반영한 코드

뉴스 분석의 공급자 실패 진단에 허용된 API 오류 유형과 고정 사유 코드만 남긴다. 공식 문서로 확인한 스키마 복잡도 및 조직·워크스페이스 지출 한도 신호를 구분하며 원문 응답·키·프롬프트는 저장하지 않는다. 기존 요청, 비용 정산, 재시도 정책은 바꾸지 않았다.

- 변경: `app/src/features/pipeline/constants.py`, `real.py`, 신규 `provider_error_diagnostics.py`, feature 내부 테스트.
- 기존 회귀 67개와 신규 12개: **79개 통과, 하위 검사 10개 통과**.
- 현재 SDK 0.122.0을 연결한 모의 HTTP 검사에서 요청의 명백한 미지원 스키마 전송이나 도구 충돌은 재현되지 않았다. 당시 HTTP 400의 상세 오류 본문은 없어 정확한 원인은 미확정이다.

## 전체 검증

- 첫 전체 실행: 8,978개 통과, 30개 실패, 1개 건너뜀. 생산 Python 413개와 HEAD는 실행 전후 동일했다.
- 짧은 새 임시 경로에서 실패가 있던 9개 파일을 재실행: **194개 통과, 실패 0개, 1개 건너뜀**. 해당 검증자의 코드 수정 없이 실패가 사라졌다. 긴 Windows 임시 경로가 관련된 검증 환경 문제였으며 보호 코드는 완화하지 않았다.
- 신규 진단 포함 전체 실행: **9,020개 통과, 실패 0개, 1개 건너뜀, 하위 검사 10개 통과**. 다만 검사 도중 다른 작업의 회사 검색 수정으로 생산 Python이 414개에서 417개가 되어 고정된 수정본의 검증으로 인정하지 않았다.
- 별도 Orca 작업공간 `report-quality-20260909`와 브랜치 `Jhh-wW/report-quality-20260909`를 기존 커밋에서 만들고 진단 변경 4파일만 옮겼다. 기존 작업공간의 회사 검색 변경은 그대로 보존한다.
- 고정 커밋 `adf4e4c88300519eb1b1a9792eecf35c9918a981`의 전체 검사: **9,016 통과, 2 실패, 3 건너뜀, 하위 검사 10개 통과**. 두 실패와 추가 건너뜀 두 건은 새 checkout에 없는 공개 DART fixture 때문이었다. 공개 XML 25개만 원본·복사본 SHA를 확인해 복원하고 해당 네 검사를 재실행하여 **4개 통과**했다. 전체·표적 실행 전후 네 snapshot의 HEAD와 생산 Python 414개 해시가 모두 같았다. 전체 실행을 다시 반복하거나 두 결과를 한 전체 성공 수치로 합산하지 않았다.

## 이전 실패 요청 비용의 대사

기존 94.41원은 실제 청구액이 아니라 미확정 보수부채였다. HTTP 400만으로 0원 처리하지 않고 다음 외부 근거를 추가 확인했다.

1. [Anthropic 공식 과금 안내](https://support.claude.com/en/articles/8977456-how-do-i-pay-for-my-claude-api-usage)의 실패 요청 비과금 정책. 성공 처리 중 연결 종료·시간 초과 예외와 구분했다.
2. 기존 성공 요청 15개의 식별자 해시가 공식 Console의 청구 로그와 모두 일치해 같은 서비스 계정을 확인했다.
3. 공식 Console의 2026-09-08 전체 시간대 사용량은 03·06·09 UTC에만 존재했다. 대상 실패 시각 10:48:59 UTC가 포함된 10 UTC에는 사용량이 없었다.
4. 내부에는 단일 요청의 `RESPONSE_RECEIVED`, HTTP 400, `BadRequestError`가 보존돼 있었다.

별도 작업자가 위 증거 결합을 검토하고 메모리 복제본에서 기존 대사 함수와 평가기의 비용 읽기를 검증했다. 총괄은 **2026-09-09 13:12:09 KST**에 원본 백업과 증거 해시를 보존한 뒤 기존 `state_machine.resolve_liability(CONFIRM_ZERO)`를 로컬 유지보수로 실행했다. 주체 `codex-run-f865a412edb2`, 사유 `provider-zero-policy-usage-20260909`의 감사 사건 1개만 추가했다. 과거 사건과 다른 테이블은 모두 불변이다.

대사 트랜잭션 확정 후 읽기 전용 검증에서 `KNOWN_ZERO`, 부채 0원, `billing_uncertain=false`와 비용 요약 정합성을 확인했다. 보고서 생성 실패라는 결과는 그대로다. 과거 체크포인트와 `ledger.json`은 당시 스냅샷으로 보존했다. 공급자가 발행한 요청별 청구 확인서를 받았다는 뜻은 아니다.

이 대사 직후 기존 평가 누적 확정 비용은 **712.57원**, 남은 미확정 부채는 **0원**이었다. 이후 우리은행 네 번의 957.54원·927.73원·871.78원·957.37원과 첫 SM 942.23원까지 **5,369.22원**이었다. `abb1af1a`의 우리은행 928.09원과 보고서 생성 없이 중단된 SM 792.91원까지 더한 현재 누적 확정 비용은 **7,090.22원**이다. 기존 20,000원 평가 예산에서 다음 새 DB에 적용할 남은 예상 예약 한도는 **12,909.78원**, 평가 건당 한도는 2,000원이다. 본조사 provider의 별도 로컬 예약 1,000원 및 요청 18회 상한도 그대로 적용되며, 평가 한도만 보고 모델 비용을 늘릴 수 없다. 공급자 청구액의 절대 상한으로 해석하지 않는다.

사용자가 API 크레딧을 직접 충전했고 2026-09-09 13:00 KST 무렵 공식 Console에서 가용 잔액 19.92 USD를 확인했다. 이 충전은 과거 요청의 비과금 판정과 별개다.

## 실제 평가와 알림

확정 manifest의 우리은행·SM·HYBE·YG·JYP·멀티캠퍼스·앱솔브랩·인텍에프에이 8사를 평가한다. 앞선 운영 관측 스위치 `ProductionObserved20260908`를 사용하되 현재 운영 배포와 완전히 같다고 주장하지 않는다. 실제 최종 품질 합격은 아직 **0/8**이며, 자동검사 통과를 보고서 품질 합격으로 대체하지 않는다.

첫 재개 실행 `resume_20260909_WOORI` / `bec0754617fd68807e1f6ff3aee2af5b`는 12쪽 PDF를 생성했다. 공급자 요청 16건 모두 HTTP 200·`KNOWN_COST`이며 확정 비용은 **957.54원**, 부채·예약 0원이다. 뉴스 분석 3요청도 모두 성공했으므로 이전 HTTP 400은 재현되지 않았다. 실행은 418초이며 프로필은 명시적으로 부분 보고서 전환을 허용하는 기존 관측 프로필이다. 이번 저장본은 `legacy-shadow-exception-v1`이며 FULL 공개 봉인을 만든 결과가 아니다.

실제 PDF 전쪽과 재무 공시·뉴스 4원문을 독립 검수했다. 2023~2025 연결 연간 영업이익·당기순이익 여섯 금액과 단위는 DART와 일치한다. 그러나 뉴스 인용 329는 매일경제 기자 본문 밖 `section#stock_story`의 ‘Powered by perplexity’ AI 해설이었다. 본문·목록 각 4건이라는 자동 지표를 품질 합격으로 인정하지 않았으며 **0/8**을 유지한다. 원래 기자 본문에 있는 2026년 6월 말 우리은행 주담대 연체 잔액 3,419억6,000만원은 보존해야 한다.

14:04 KST에 해당 서버의 요청·단계 정산과 소유 프로세스 생성시각을 검증하고 launcher 14332, Python 25116·18992만 종료했다. 원본 DB·보고서·원장·체크포인트를 보존했다.

뉴스 보조 영역을 모든 추출 경로에서 제외하는 수정은 관련 **389개 검사**와 독립 영향 영역 **124개 검사**를 통과했다. 독립 전체 447개 중 처음 실패한 짧은 메타 재유입 한 건도 후자의 재검증에서 해결했다. 실제 329 기사에서 AI 해설은 제거하면서 기자 본문의 은행 금액·기간은 보존했고 나머지 세 기사 추출 SHA는 수정 전과 같았다. 겹치는 통과 수를 합산하지 않는다.

표의 `source_cites`는 `row_cites`나 FULL manifest가 없으면 처음 저장할 때 빠지고 있었다. 비어 있지 않은 전체 출처는 독립적으로 저장하도록 수정했고, 저장 전 통과→실제 DB 저장 후 실패 반례를 재현한 뒤 관련 **80개 검사**가 통과했다. 독립 검토에서도 실제 작성기의 이름표·뉴스표가 최초 JSON·새 DB·출고 검사·PDF까지 유지되고, 빈 기본값의 저장 bytes 및 FULL 행/셀 결속·변조 차단을 포함한 **4개 검사**가 통과했다. 이미 출처를 잃은 과거 JSON은 추정 복구하지 않는다. PDF는 200pt 이내 짧은 글 전용 장의 제목·문단을 함께 넘기도록 수정했으며 기존 디자인·본문·공개 봉인 관련 **76개 검사**와 실제 마지막 장 문단의 배치 조각 검수가 통과했다. 실제 새 보고서의 저장·재출력과 전체 시각 검수는 후속 실행에서 확인한다.

Orca 알림 v2는 관리형 작업자의 사용자 인계 복구까지 13파일 **178개 검사**, 실제 IPC·SQLite 20경계 검사와 전체 Node·CLI·Web 타입검사 및 정적 빌드를 통과했다. 공식 설치판의 같은 버전 네이티브 파일을 읽기 전용으로 복사하고 공식 준비 런타임 재사용 절차로 Windows 패키징을 완료했다. 별도 프로필에서 실제 패키지의 main/renderer 시작·정상 종료를 확인했고 원래 설치 앱은 유지했다. 후보는 **미서명·미설치**, 설정 권한 조회만 실제 GUI에서 검증했고 OS 알림 전달 E2E는 미검증이다.

v3는 완료된 external/관리형 작업자의 실제 입력과 명시적 Codex 프롬프트가 함께 확인될 때 메인 알림을 복구한다. 정리용 소유권·DB 스키마를 바꾸지 않았고 **18파일 224개 검사**, 전체 타입검사·lint를 통과했다. Windows v3 후보 패키징은 완료됐으며 175,078,119바이트 설치 후보의 SHA-256은 `51b5cb780f943269f0fe8dbaacdc73f8baf5e6865aa1fe6c86121058220f3949`다. 미서명·미설치 상태다. 격리 GUI 한 번은 관측 도구가 Windows cmd 래퍼를 앱 본체로 잘못 단언해 검증을 마치지 못했다. 이를 제품 시작 실패로 판정하지 않았으며 해당 소유 프로세스들은 모두 정상 종료됐다. 수정된 관측 도구의 계보 검사 7개는 통과했으나 v3 renderer·권한 IPC·OS 알림은 실제 GUI에서 확인되지 않았다. 문맥은 메모리 안에만 있어 DB 재열기 후 새 입력·프롬프트가 필요하며, 인계 이후 늦게 도착한 완료의 턴 ID 부재·원격·공식 업데이트 후 개인 패치 지속성은 여전히 한계다.

## 두 번째 실제 평가와 의미 범위 보완

고정 커밋 `4dbb870f7ff8b3fe4b5428b019c23ffe85eb8a04`의 `resume_20260909_widgetfix_WOORI` / `8d255d526bb1fa88a0478e958622fc53`은 405.6초에 8쪽 PDF를 생성했다. 공급자 17요청은 모두 HTTP 200·`KNOWN_COST`, 확정 비용 **927.73원**, 예약·부채 0원이다. 뉴스 분석 4요청 92.91원, 작성 10요청 475.68원, 도식 2요청 201.97원, 검수 1요청 157.17원이다. 실행 중 청구 상태와 최종 정산을 구분했다. 15:06 KST에 단계 세 개의 정산과 소유 PID·부모·생성시각을 검증한 뒤 시험 서버를 종료했다. 기존 DB·PDF·체크포인트를 보존했다.

새 저장본의 이름표 출처 16개와 뉴스 출처 2개가 최초 저장·DB 왕복·출고 검사에 유지됐고, 메모리에서 다시 만든 PDF의 bytes도 원본과 동일했다. 8쪽 전체 시각 검수에서 겹침·잘림·마지막 짧은 장 문단의 분리를 발견하지 않았다. PDF SHA-256은 `16bf2f7d76a5bd02f959c51213a25557ab7ac11719ea9ebcd69d0d375dbfde10`이다. 기존 관측 프로필의 부분 보고서·`legacy-shadow-exception-v1`이며 FULL 공개 봉인 통과가 아니다.

2023~2025 연결 연간 영업이익 33,170·40,693·35,207억원과 당기순이익 25,151·30,469·25,821억원은 공시의 항목·기간·단위와 일치했다. 과거 잘못 결합했던 신탁 수익 소계 2,123억원과 실제 운용수익 1,977억원도 분리해 확인했다. 기사 두 건은 전자신문 9월 9일 라이징리더스 지원 계획과 연합뉴스 9월 8일 인정가게 사진 기사다. 각각 본문·별도 목록·날짜·회사·사건·원문 해시를 확인했다. 지원 예정 금액은 매출이나 이미 집행한 금액으로 해석하지 않는다. 이전 매일경제 AI 해설은 새 최종 보고서에 없으나 해당 URL의 탈락 단계가 관측되지 않아 이번 유료 실행만으로 필터의 인과 효과를 주장하지 않는다.

최종 품질 합격은 **0/8**을 유지했다. 원문이 ‘선도해나가고자’라고 밝힌 목표를 현재 성과처럼 인용한 문장, 특정 상품의 BB+ 조건을 기업금융 채널 전체로 넓힌 문장, 서비스 출시와 경영목표를 공식 조직 의사결정 방식으로 추론한 문장을 발견했기 때문이다. 원문과 결과를 보존하고 작성·기존 검수 프롬프트에 계획·적용 대상·공식 조직 설명의 구분 및 장별 책임을 추가했다. 같은 검수 호출 뒤 명확한 형태의 반례만 추가 차단하는 composer 내부 가드와 닫힌 진단을 연결했다. 새 모델 호출·재시도나 기사 개수 할당을 추가하지 않았다.

초기 통합 뒤 composer와 관련 진단·전달 검사는 **1,136개 통과**했다. 이후 독립 검토는 공개 원문 인용 10개의 exact SHA를 복원하고 실제 검수 함수로 추가 우회 3종을 재현했다. 첫 표적 결과는 **10개 통과·4개 실패**였으며 실패 기대값을 유지했다. 슬롯 없는 문화 요약, 다른 회사의 현재 실적을 통한 계획 면제, 도식 두 경로의 칸 사이 조건 결속이 대상이다. 요약의 문화 경계를 적용하고 명시된 보고 대상 주어와 생략된 주어를 분리했으며, 도식은 원래 칸 배열로 인접 대상·조건을 추가 대조한다. 수치·시점용 세미콜론 문자열과 기존 JSON 검증은 그대로다. 미지원 응답 필드를 요구하던 미사용 제안 상수는 제거했다. 최종 영향 경로 **350개 검사 통과**, 검사 중 생산 Python과 HEAD 불변을 확인했다. 이후 공개 인용 fixture를 feature 테스트 안에 함께 저장해 개인 임시 폴더 없이 실행되도록 했고 해당 **14개 검사 통과·건너뜀 0개**를 확인했다. 임의 동의어·긴 복합문·출처 공식성 전체를 이 좁은 가드가 증명한다는 뜻은 아니며 후속 실제 평가가 필요하다.

## 세 번째 실제 평가와 도식·원문 제목 보완

고정 커밋 `4df4db49730d4d9620f92c9a31b0d4a8dcb44dec`의 `resume_20260909_semantics_WOORI` / `1f4f203fe666df3cfb06ad09dd30e763`은 403.7초에 11쪽 PDF를 생성했다. 공급자 17요청 모두 HTTP 200·`KNOWN_COST`, 확정 비용 **871.78원**, 예약·부채 0원이다. 뉴스 92.64원, 작성 463.18원, 도식 76.24원, 검수 239.72원으로 합계가 일치한다. 이번에도 관측 프로필의 부분 보고서이며 FULL 공개 봉인 통과가 아니다.

독립 검수는 최초 저장·DB 왕복·출고 검사와 메모리 PDF 재출력의 일치를 확인했다. 이름표 출처 16개와 뉴스 인용 3개가 유지됐고, PDF SHA `17a1a25153c899d3a9d8f3f7300baaf9705989be0a830596193d4fe739dd0951`은 재출력과 같다. 11쪽 전체에서 잘림·겹침·빈 상품 카드를 발견하지 않았다. 연간 연결 재무 여섯 값과 기간·환산도 맞다. 별도 뉴스 검수는 기사 두 URL의 본문·목록·출처·원문 SHA를 확인했다. `news_distinct_event_count=3`은 독립 사건 세 건을 뜻하지 않는다. 두 기사는 같은 8기 선정 발표를 다루며, 세 번째 인용은 2023년 이후 지원 누계 배경이다.

앞선 목표의 현재형 단정, 특정 상품 BB+ 조건의 채널 전체 확대, 상품 출시에서 문화 추론한 사례는 이번 결과에 나타나지 않았다. 그러나 다음 세 문제가 있어 **합격 0/8**을 유지했다.

- p3 수익 도식: 원문85의 서비스 출시·제휴·통신료 절감에 없는 이용료/수수료 지불과 교차판매 수익을 추가했다. 원문240의 신탁 관리 수수료는 명시된 정상 근거이므로 함께 삭제할 대상이 아니다.
- p1 요약·p5 본문과 도식: 원문224/282 직전의 ‘8) 러시아우리은행’ 제목이 수집 조각에서 빠져 현지 법인의 영업 제한이 은행 전체 또는 해외영업 일반처럼 읽혔다. 두 조각의 exact SHA는 `235973f0e4a976d7ebba9f90071780b035c759a0582b6935430ff843680f500a`다.
- p8 문화 도식: 원문219의 미래 사업목표를 현재 일하는 원칙으로 옮겼고 표시 계층은 표 머리말만 보고 ‘전사 공통 공식 기준입니다’를 추가했다. 실제 준법·위원회·위험관리 절차는 정상 대조로 유지해야 한다.

도식 검수의 ‘원문이 말하지 않은 상대에게 화살표를 그은 줄만 거짓’이라는 좁은 지침을 바로잡고, 값이 있는 각 칸의 주장·과금·반복수익·대상·시점과 관계를 함께 검수하도록 두 진입점에 같은 안내를 전달했다. 작성 지침도 고객 행동만 확인되면 과금을 채워 넣지 않으며 고객 혜택·비용 절감을 수수료 수익으로 바꾸지 않도록 보완했다. 과금 어휘 사전으로 정상 근거를 자동 승인하거나 모든 동의어를 삭제하는 가드는 추가하지 않는다. 의미적 개선 여부는 후속 실제 평가로 확인해야 한다.

문화 카드와 정형 사실 카드의 기본 한계 문구를 ‘인용 자료에 나타난 범위로 한정합니다’로 바꿔 표시 계층의 전사 범위 단정을 제거했다. 해당 단계의 기존 검사 159개가 통과했다. 원문 제목은 20자 미만의 명시 제목과 직후 적격 문단만 실제 연속 범위로 보존하며 새 section 객체를 늘리지 않는다. 기존 문서·후보·문자 상한과 short 관측을 유지하고 같은 무분류 장문 안에 포함된 짧은 제목의 중복 범위만 제외했다. 실제 두 문서 × 두 호출에서 기존 회사 분류·원문 해시·위치·usable range·잘림 없음이 확인됐으며 제목 포함 SHA는 `3d739fe0dd83f8cde049d96c41517d8e63251b86f674c950cc1837a895baa59d`다. 해당 기능 264개와 app 출처·전송 경계 192개가 통과했다.

문화 도식에는 같은 가치 표현에 결속된 미래 사업목표를 현재의 원칙처럼 표시한 좁은 사례를 제한하는 `culture_flow_problem`을 연결했다. 실제 공식 목표를 거짓으로 분류하지 않으며 원칙 칸에 계획을 명시한 대조와 준법·위험관리 절차는 보존한다. 기존 포함 단위 검사 58개가 통과했고 공개 원문 네 건은 기능 테스트 옆에 저장했다. 두 검수 진입점·진단 유무·정상 두 행 보존·명시적 계획 대조를 포함한 새 통합 6개와 기존 관련 35개도 통과했다.

최종 파일 동결 후 composer·공통 품질 진단·표시·진단 전달의 **1,586개**, 영문 함수명으로 정리한 제목 계약 **23개**가 통과했다. 종료 코드는 모두 0이며 생산·테스트·JSON 파일 집합과 HEAD가 검사 전후 동일했다. 기존 라이브러리 경고 11개가 있었고 앞선 겹치는 통과 수를 합산하지 않는다. 독립 검토에서 두 도식 진입점에 실제 85/240 원문과 공통 지침이 전달됨을 확인했지만 모의 ‘전부 참’ 응답은 85도 남긴다. 과금의 의미 판단을 모델에 맡긴 설계의 한계이며 새 모델 실패 관측이나 품질 합격을 뜻하지 않는다. 새 유료 보고서 재평가는 아직 완료하지 않았다.

평가 서버는 16:06:52 KST의 최종 영수증에서 소유 프로세스 모두 부재, 8020 listener 부재, DB SHA `b6e997f9eea675e005c889ed6ae6c81a5f58ac2ab1ad573fbbc67561ebf0d4b7` 불변을 확인했다. 첫 종료 시도의 배열 파싱 실패와 두 번째 종료 중 연쇄 종료에 따른 중단을 별도 기록했다. 사용자 Orca는 그 전에 직접 업데이트돼 PID 27396·1.4.198이었으며 이 프로세스는 종료하지 않았다.

Orca v3 1.4.197 후보의 격리 GUI 재시도는 초기 main/renderer/preload, 실제 권한 조회 IPC, 사용자 턴 listener 연결·정상 종료 시 해제를 확인했다. 원래 설치판은 사용자 업데이트로 1.4.198이어서 공식 태그의 커밋 `e0826956fcfc532f5a1e55b5e081f2e57e553c43`에 별도로 이식했다. 기존 명시 컬럼 SQL을 보존한 17파일 변경은 고유 회귀 252사례와 Node·CLI·Web 타입·lint 및 공식 빌드·Windows 패키징·네이티브 검사를 통과했다. 1.4.198 설치 후보는 **179,467,830바이트·미서명·미설치**, SHA `b8f8c594f427d9b34905337bb6aeeb9f1254676d59312aad0344ae5fa2b0a6ac`다. 정적 main과 ASAR main SHA `63e279449ee6b6d62c0707555fd253799ad8c86cb229d83dff1d7d7d4d003711`이 일치한다.

1.4.198 후보도 새 격리 프로필에서 숨김 GUI를 한 번 실행해 준비·실제 권한 IPC·listener 연결/정상 종료 시 해제를 확인했다. 사용자 설치판 PID 27396과 지정 설정·인증·후보 파일은 유지됐다. 자동 읽기 전용 업데이트 확인만 있었고 다운로드 캐시는 없었다. 활동 중인 원본 orca-data 전체 해시 변경을 전체 사용자 상태 불변으로 숨기지 않았다. 현재 앱에 패치를 적용한 것은 아니며 OS 알림 전체 E2E·지연 턴 식별·원격·재시작·업데이트 후 지속성 한계는 남는다. 근거는 원래 checkout의 `.local-artifacts/resume-20260909-orca-198/report.md`와 `resume-20260909-orca-gui-198/report.md`다.

이전 실행·비용 대사·Orca 후보의 상세 증거는 원래 작업공간 `C:/Users/jh-wo/.claude/workspace/기업분석2`의 Git에서 제외된 `.local-artifacts/resume-20260909-*`에 있다. 새 checkout의 같은 디렉터리에 실제 세 번째 평가의 `WOORI-semantic-quality.md`, `WOORI-semantic-news.md`, `evaluation-launch/stopped-semantic.json` 등을 보존했다. 푸시·배포·현재 Orca 설치 변경은 하지 않았다.

## 네 번째 우리은행 및 첫 SM 실제 평가

고정 커밋 `f29f4e90db6de920470890f663c25e648b3a824a`의 새 서버에서 우리은행과 SM을 순서대로 생성했다. 두 실행의 설정 SHA는 `2d9e1a5fd7afb4fa6f79e5f9e91b075d1cd3ed73f388adbc28ece3a01e53feec`, source receipt SHA는 `e7245513203990415f153828481ad32cf8addcdda895c4697757a2a6dfb6857b`다. 설정의 clean·코드 신원과 검사 대상 1,070파일의 SHA를 완료 후에도 대조했다. 요청 프로필과 실제 부분 보고서·`legacy-shadow-exception-v1`을 구분하며 FULL 공개 봉인 통과로 보지 않는다.

| 사례 | batch / run | 시간·PDF | 확정 비용 |
|---|---|---|---:|
| WOORI | `resume_20260909_headingflow_WOORI` / `404a9f930e994376b83b75d20da5aae8` | 405.5초·11쪽 | 957.37원 |
| SM | `resume_20260909_headingflow_SM` / `39d2d365463f9c59eda73b15f3e7c81d` | 478.0초·7쪽 | 942.23원 |

각 18요청 모두 HTTP 200·KNOWN_COST이며 원장·AI 사건·평가기 합계가 같다. 두 실행 각각 예약·미확정 부채 0원, 세 phase 계정 SUCCEEDED를 확인했다. 우리은행의 작성·검수·도식·뉴스 실제 모델은 모두 `claude-haiku-4-5-20251001`이었다. 서버는 listener 종료 후 자식들이 연쇄 종료됐고, 17:14:55 KST 최종 확인에서 소유 인스턴스 5개와 8020 listener 모두 부재였다. 종료 중 stub 조회 경합으로 초기 스크립트가 중단된 기록도 남겼다. 사용자 Orca PID 27396은 유지했다. 종료 후 DB SHA는 `77d9c57157e51c565ed5932662b46d8bfa9024ec63f3d2c309ffdd9271e1b520`이다.

**우리은행: 수정 필요.** 이전 NIM 고객요금·원금/지급이자의 수익 표시·MVNO 과금/교차판매 문구는 없어졌다. 러시아우리은행 제목을 포함한 새 원문225를 받은 본문도 법인 범위를 지켰다. 그러나 도식에는 법인명이 다시 빠졌고, 특정 대출의 보증·신용 조건을 다른 상품이나 기업금융 전체로 넓힌 설명, 근거 없는 반복 수익, 계획의 현재화, 디지털 고객서비스를 조직문화로 승격한 문장이 남았다. 재무 숫자 여섯 값과 누적 증감률은 맞지만 신용비용을 이익 감소의 ‘주요 원인’으로 단정한 인과는 원문4/83의 범위를 넘는다. 정상 신탁 수수료·HR/CDP·윤리·이사회 근거는 구별했다.

우리은행 뉴스는 아시아경제의 2026-09-09 중견기업 선정·지원 착수 기사 한 건이다. 본문·목록·출처326의 exact SHA `f63a22a85cd3d01801010018d802ffbc70be98ee24637b79bcc023e245e31bfd`와 기자 문맥·PDF 링크를 확인했고 새 뉴스 의미 오류는 발견하지 못했다. 두 제외 조각은 이번 실행의 검수 후 미반영 기록까지만 확인했다. PDF 전쪽에 잘림·겹침·빈 카드가 없고 새 문화 한계 문구 네 개가 정상이며 최초 저장·DB 왕복·메모리 PDF 재출력이 같다. PDF SHA는 `9cbac37363fd85a5e062f5e6333640981482717929abec11d51220f1f6c179bf`다.

**SM: 수정 필요.** 매출·영업이익·당기순이익의 2023~2025 연결 연간 아홉 값과 누적 증감률 세 개가 이번 공식 API·공시와 일치했다. 2024 당기순이익 818,849,820원은 양수이고 억원 표시 8이 맞다. 저장·출처·PDF 재출력도 같고 전쪽의 글자 잘림·겹침은 없었다. 다만 4쪽 성장 전략은 확인 문장 0개라는 전역 사유를 장에 전달하지 않아 제목만 출력됐다. 판매조직을 기획·제작 담당으로 확장하거나 플랫폼 보유를 자체 개발로 바꾼 문장, 일반 회계처리를 문화 장에 넣은 설명도 남았다. 자회사 주체·아티스트 배출 이력·수익 배분·로열티의 정상 근거는 유지했다.

SM 최종 뉴스244의 NCT127 재계약 발표 자체와 발행일·회사·본문/목록/exact SHA는 맞다. 그러나 언론사 `a.stock` 안의 별도 `span.price` 시세 `(80,200원 ▼2,500 -3.02%)`까지 본문과 목록에 혼입됐다. robots 허용 확인 후 받은 공개 HTML과 선택 문장의 SHA가 일치하며 현재 article 추출에서도 재현됐다. 회사명과 기자의 정상 숫자를 보존하면서 명시적 시세 자식만 제외해야 한다. PDF SHA는 `743342f5309de546780eff8b99ca46a7403c00d5f339c08b02632460de3c2129`다.

최종 실제 품질 합격은 **0/8**이다. 이번 두 결과는 원문과 출처 연결·재무 검산·저장·표시의 정상 부분과 의미·시세·빈 장 결함을 분리해 기록했다. 다음 수정은 일반적인 검수 응답 구조와 원문 중복, 빈 장의 정확한 사유 전달, 명시적 인라인 시세 컴포넌트 경계를 대상으로 한다. 기사 수 하한 추가·예산 완화·과거 결과 변조로 해결하지 않는다.

근거는 새 checkout의 `.local-artifacts/resume-20260909-WOORI-headingflow-{news,quality,semantics}/`, `resume-20260909-SM-headingflow-{news,quality,semantics}/`, `resume-20260909-evaluation-launch/settled-headingflow-{WOORI,SM}.json` 및 `stopped-headingflow.json`이다. 정산 도구는 읽기 전용 SQLite와 지정 실행만 사용하며 전체 활동 DB의 파일 해시 불변을 요구하지 않는다.

## 인라인 시세 제거와 검수 응답 개선

뉴스 본문에서는 명시적 `a.stock[data-testid="stock"]` 내부 `span.price`만 제외하도록 수정했다. 회사명·기사 본문의 숫자·괄호·일반 price 요소는 유지한다. 네 추출 경로의 마크업에 같은 경계를 적용하며 같은 페이지에서 관측한 컴포넌트 전체의 정확한 평문 복사본도 재유입시키지 않는다. 구조가 없는 시세를 숫자 모양으로 추정해 삭제하지 않는다.

기능 회귀 **392개(신규 33개 포함)**가 통과했다. 총괄은 신규 시험의 다른 feature 직접 import를 같은 feature의 기존 추출 helper 재사용으로 정리하고 **33개 통과**를 다시 확인했다. 실제 SM 보존 HTML의 article 본문은 520자에서 496자로 바뀌며 회사명·재계약 문장은 남고 시세만 빠졌다. 새 추출 SHA는 `35741c989e2df03fee2e579ebdc2163b1e49b0a7fa6e75f5ddf47c44109040af`다. 원본 HTML·과거 보고서·출처 SHA를 소급 변경하지 않았고, 기존 무세미콜론 앰퍼샌드 파서 한계는 이번 수정 범위 밖이다. 근거는 `resume-20260909-inline-quote-fix/report.md`와 `resume-20260909-root-news-review/receipt.json`이다.

본문·도식 검수 응답 구조와 빈 장 표시 구현은 완료했고 최종 통합 검사를 준비하고 있다. 본문 검수는 인용 번호를 포함한 16자 이내 `근거대조`를 판정보다 먼저 요청한다. 이 설명을 파서의 승인 근거나 숫자 검증 자료로 사용하지 않으며, 기존 판정·인용 소유권·숫자·기간·요약 검수 경계는 유지한다. 판매를 제작으로, 보유를 자체 개발로, 계획을 현재 성과로, 부담 요인을 주요 원인으로 바꾸는 추가 주장은 직접 근거가 없으면 거짓으로 판정하도록 안내했다. 제어된 응답으로 실제 반례 5종의 두 검수 경로를 확인했으며, 모델이 실제로 같은 판정을 내렸다는 증거는 아니다.

도식 검수는 같은 인용 번호의 원문을 한 번만 전달하고 각 행의 인용·값이 있는 칸·숫자 대조 정보를 유지한다. 실제 우리은행 저장 도식 20행·인용 25개 대조에서 프롬프트는 47,876자에서 19,670자로 줄었고 각 인용의 원문 SHA와 행 소유권이 같았다. 이는 글자 수 비교이며 공급자 토큰 절감 실측은 아니다. 서로 다른 인용 번호는 원문이 같아도 합치지 않는다. 40자 이내 `대조근거`를 먼저 요청하며 파서는 이를 승인 자격으로 사용하지 않는다.

출력 배정은 작성 4,000을 유지하고 본문 검수 8,000→12,000, 도식 512→2,048로 조정했다. 실제 요청과 같은 입력·출력 상한으로 먼저 예약하며, 남은 예산이 모자라면 요청을 보내지 않는 기존 보호를 유지한다. 모델은 Haiku이고 본조사 1,000원·평가 2,000원·18회 한도는 그대로다. 성공 응답의 종료사유와 출력상한은 원시 단계 기록에 허용된 값만 남긴다. 관련 비용 경계 15개 검사가 통과했다. 기존 우리은행 비용으로 가정한 72개 시나리오 중 43개는 마지막 선택적 요약 검수 예약에서 막혔으며, 이는 실제 실패율 예측이 아니다. 그 경우 이미 검수한 본문으로 요약하는 기존 대체 동작을 사용한다. 출력 배정만으로 응답 완성이나 품질을 보장하지 않는다.

숫자·기간 대조 후 본문 설명이 전부 제외되면 자료 부재와 구별한 안내를 남긴다. 과거 v2 부분 보고서의 빈 장에는 장별 명시 사유가 있으면 그대로 표시하고, 없으면 원인을 확인할 수 없다는 중립 문구를 표시한다. FULL·봉인 보고서와 v1에는 적용하지 않으며 안내는 검증된 사실 수를 늘리지 않는다. 실제 SM의 메모리 재출력은 7쪽을 유지하면서 성장 전략 장에 안내만 추가했고 원본 JSON·PDF는 보존했다. 총괄은 해당 쪽의 안내 배치에 겹침·잘림이 없음을 확인했다.

오래된 v1 PDF 고정값 실패는 `f29f4e90`에서 승인한 문화 안내 두 곳의 변경 때문이었다. 과거 문화 함수만 메모리에서 복원하면 이전 길이·PDF·낱말·배치 네 고정값이 정확히 재현됐고, 변경 쪽의 공통 407단어 좌표·폰트가 같았다. 이 근거로 두 시험 파일의 고정값과 설명만 갱신했으며 기존 28개 검사가 한 번에 통과했다. 두 배정의 `agent_prompt_stalled` 이후 완료 통지가 폐기된 권한으로 거절되어, 총괄이 편집 종료·idle·28개 통과 영수증·현재 파일 SHA를 확인하고 작업을 수동 인수했다. 정상 완료 통지 수락으로 소급 표시하지 않는다.

독립 읽기 검토는 생산 20개를 포함한 29파일의 불변을 확인했고 검토 범위에서 새 기능 차단 결함을 발견하지 못했다. 신규 시험의 함수명 15개를 영문으로 정리했으며 이름 외 AST와 바이트는 보존했다. 실제 웹 GET을 포함한 통합 검사를 진행한다. 후속 실제 생성·품질 판정은 아직 완료하지 않았으며 합격은 계속 **0/8**이다.

우리은행·SM 재무 API 출처의 누락된 공시일 전달도 구현했다. 성공한 API 응답 전체 행과 공식 공시의 회사·연도·연간 보고코드·접수번호가 같고, 해당 응답으로 만든 정확한 표시 조각일 때만 별도 공시일을 전달한다. 목록 응답에 보고코드가 없으면 기존 수집기의 연간 선택 표식과 공시 제목·연도를 함께 확인한다. 다른 회사·분기·접수가 섞인 경우, 날짜가 없거나 잘못된 경우에는 날짜를 만들지 않는다. 실제 조회일과 공시일을 구별하고 원문·인용 번호·금액은 유지한다. 새 날짜가 있는 근거 packet은 버전 5로 날짜까지 봉인하고 날짜가 없는 기존 packet은 버전 4와 필드 생략을 유지한다.

날짜 관련 초기 인접 회귀는 **63개 통과**, 시험을 기능 내부와 전역 웹 연결 위치로 정리한 후 신규 3파일은 **41개 통과**했다. 중복이 있으므로 합산하지 않는다. 보존된 두 API 원문과 검증된 XML 제출일로 구성한 오프라인 입력은 각각 2026-03-13·2026-03-17을 공시일로 전달했고 원문 SHA와 기존 보고서는 유지했다. 원 DART 목록 응답이 따로 보존되지 않아 검증된 메타데이터로 시험 입력을 재구성한 범위이며 실제 목록 재생·새 수집 성공으로 주장하지 않는다. 조회일은 근거가 없으면 빈 값으로 둔다. 최종 생산 8파일·시험 3파일을 동결한 뒤 통합 검사에 인계했다.

증거는 `.local-artifacts/resume-20260909-body-review-fix/`, `resume-20260909-diagram-budget-fix/`, `resume-20260909-diagram-source-crossreview/`, `resume-20260909-review-output-budget/`, `resume-20260909-empty-section-fix/`, `resume-20260909-v1-golden-{audit,refresh}/`, `resume-20260909-final-independent-review/`, `resume-20260909-financial-source-date-audit/`에 보존했다. 겹치는 검사 수를 합산하지 않으며 최종 고정 소스의 통합 검사 결과는 별도 영수증으로 기록한다. 푸시·배포·설치 변경은 하지 않았다.

## 통합 검사 마감과 실제 평가 준비

동결된 소스에서 실제 웹 GET·저장·봉인·뉴스·본문·도식·재무 날짜·비용·PDF를 포함한 24개 검사 경로를 실행했다. **2,738개 통과·1개 실패·39개 경고**였고, 1,217파일과 HEAD는 검사 전후 동일했다. 실패는 v1 HTML 기준 파일의 문화 안내 두 곳이 `f29f4e90`의 승인 문구를 반영하지 않은 문제였다. 기준 HTML의 두 문구와 해당 텍스트 지문·시험 이름·설명만 갱신했으며 비교 assert와 정규화 로직은 유지했다.

관련 파일 전체 **11개가 통과**했고, 새 시험 이름을 영문으로 바로잡은 뒤 별도 `resume-20260909-html-golden-final-v2/`에서 11개를 한 번 더 실행해 모두 통과했다. 원 통합 검사와 비교해 바뀐 파일은 기준 HTML과 그 시험 파일 두 개뿐이며, 나머지 1,215파일은 같은 내용이다. 전체 2,739개를 한 번에 다시 실행한 결과로 표현하지 않는다. 원래 실패 영수증 `resume-20260909-review-final/receipt.json`은 그대로 보존하고, 통합 범위의 소스 불변과 관련 파일 재검사 결과를 함께 판단 근거로 쓴다.

HTML 갱신 작업자의 최초 명령은 앱 상대경로에 `app/`을 중복해 0개 검사·종료 4였으며, 작업자가 그 실행의 로그·XML·영수증을 삭제한 뒤 올바른 경로로 실행했다. 삭제된 바이트는 복구하거나 재구성하지 않았다. 이 이력은 `resume-20260909-html-golden-refresh/_naming-and-run-history.md`와 총괄의 `resume-20260909-html-golden-root-review/initial-invocation-observation.json`에 명시했다. 이후 영문 이름 교정 검사는 새 디렉터리에서 실행했고 기존 통합 실패·성공 영수증을 보존했다.

사용자 요청에 따라 총괄은 Astra Codex가 유지하고, 서브 작업은 Claude Code Sonnet(중간 사고) 두 자리와 Opus(높은 사고) 한 자리로 전환했다. 다음 실제 실행을 위한 저장·PDF·뉴스·재무·응답 진단 대조 도구는 지정 회사와 실행 신원을 읽기 전용으로 확인하며, 옛 사례의 통과를 새 결과의 품질 합격으로 이월하지 않는다. 현재 실제 품질 합격은 **0/8**, 캠페인 확정 사용액은 **5,369.22원**, 잔여는 **14,630.78원**이다. Anthropic 계정의 현재 지갑 잔액은 로그인되지 않은 별도 브라우저에서 확인하지 못했으며, 과거 관측값을 현재 잔액으로 쓰지 않는다.

## `abb1af1a` 실제 재평가 결과

41파일을 `abb1af1a9876cebaf9f3aed028d82185fa2034b0`에 저장한 뒤 새 서버에서 두 사례를 순서대로 실행했다. 평가 폴더는 `app/.local_evaluation_runs/20260909_211255_372552fafacc812e0ed632dc`, 설정 SHA는 `5b2707da65c5b5d5c6983bf35068c9a79d50f687262bbe450ca6d30fa3094244`, 생성 소스 지문은 `eb1d2648e6202f6daf1a3787b5b6db21ec181a50b53ab09c13c65d21a9f20045`다. 두 실행 정산 때 현재 파일 1,217개의 SHA가 마감 검사와 같고 Git이 깨끗함을 확인했다.

| 사례 | batch / run | 실제 결과 | 확정 비용 |
|---|---|---|---:|
| WOORI | `resume_20260909_reviewfix_WOORI` / `79f83d7dba5a80ec0708183f95aea79a` | 515.8초·10쪽 보고서·의미 수정 필요 | 928.09원 |
| SM | `resume_20260909_reviewfix_SM` / `05833f354a6ec7f9676046b714c4f2f7` | 433.6초·게이트 중단·보고서/PDF 없음 | 792.91원 |

우리은행의 18요청과 SM의 15요청은 모두 HTTP 200·KNOWN_COST이며 원장·AI 비용·평가 결과 합계가 일치했다. 예약과 미확정 부채는 두 사례 모두 0원이다. `phase=SUCCEEDED`는 해당 비용 계정 정리가 끝났다는 관측이며 SM 보고서 생성 성공을 뜻하지 않는다. SM 결과를 완료 보고서 전용 정산기로 검사하지 않고 게이트 결과의 빈 보고서 신원·생성 파일 부재·확정 비용을 확인하는 별도 읽기 전용 도구로 대조했다. 그 도구의 최초 실행은 빈 `ledger.report_id`를 실행 ID와 비교해 실패했으며, 최초 스크립트와 실패 관측을 보존한 뒤 실행 신원을 체크포인트와 지정 run의 DB 원장으로 확인하도록 수정했다.

우리은행은 저장·DB 왕복·재출력 PDF·인용 대상이 일치했다. Claude가 10쪽을 확인했고 총괄은 4쪽 그래프·본문과 10쪽 출처 표를 추가 확인해 잘림이나 겹침을 발견하지 못했다. 재무 API 출처 7에는 공시일 **2026-03-13**이 실제 전달됐고 조회일은 빈 값으로 유지됐다. 재무 6칸·누적 증감률 6.14%와 2.66%는 이번 실행 보존 API와 일치했다. 보고서 기준일을 조회일로 간주한 작업자 표현은 별도 정정했으며, FULL 봉인 성공으로 표시하지 않는다.

우리은행 본문 검수는 `12000 / end_turn`이었지만 도식 검수 두 응답은 모두 `2048 / max_tokens`였다. 관계 도식이 사라진 것은 잘못된 관계를 의미적으로 판별했다는 증거가 아니다. 본문에서는 개별 상품의 신용 자격을 기업금융 전체의 차등 금리 정책으로 넓힌 설명과 신용비용 부담을 영업이익 감소의 주요 원인으로 바꾼 설명이 남았고, 무관한 회계정책·목차 제목에 결속된 인용도 발견됐다. 신탁 수수료·윤리·준법·위원회·회사 원문에 명시된 원비즈 자체개발은 정상 근거로 유지했다. 문단 끝 인용이 적용되는 앞 문장을 `lines`의 개별 줄만 보고 인용 누락으로 분류하지 않도록 감사 표현도 구별했다.

우리은행 뉴스 5개 URL의 공개 HTML을 robots 확인 후 확보했다. 기업별 최대 300억원과 총 5,480억원은 개별 한도와 총액으로 원문에서 구별되므로 금액 불일치가 아니다. 한국경제의 두 URL은 같은 날 같은 ATM 통계와 내용을 재구성한 기사였으며 보고서에서 유사 내용을 두 번 서술했다. 다른 URL이라는 사실을 별개의 사건으로 취급하지 않는다. 과거 원문·보고서와 뉴스 영수증은 보존했다.

SM은 본문 검수의 입력 106,878토큰·출력 12,000토큰에서 `max_tokens`로 끝난 뒤 재요청 예약이 `request_budget_exhausted`로 막혔다. 공급자 크레딧 오류나 19번째 요청 차단으로 바꾸어 설명하지 않는다. PDF·저장·뉴스 시세 제거·재무 공시일의 새 보고서 검수는 실행할 결과가 없어 미판정이다. 동일 입력을 곧바로 유료 재시도하지 않았다.

두 실행의 정산 후 서버를 종료했다. 최초 종료 순회는 프로세스 신원 재조회 값이 달라 중단됐고, 이어진 독립 조회에서 이번 소유 프로세스 5개와 8020 listener 모두 부재를 확인했다. 사용자 Orca PID 27396은 유지했다. 증거는 `resume-20260909-evaluation-launch/{settled-reviewfix-WOORI,settled-reviewfix-SM,started-reviewfix,stopped-reviewfix}.json`, `resume-20260909-WOORI-reviewfix-{quality,news,news-public,semantics}/`, `resume-20260909-SM-reviewfix-quality/`에 보존했다. 현재 합격은 계속 **0/8**이며 다음 수정은 본문 검수의 뉴스 보조근거 범위와 응답 분량을 대상으로 한다.

## 검수 응답 분량과 뉴스 중복 후속 수정

본문 검수의 flat 경로에서 뉴스가 하나라도 있으면 모든 장의 비인용 공식 근거를 더하던 범위를 줄였다. 각 후보의 실제 장을 먼저 확인하고, 뉴스를 직접 인용한 후보가 속한 장에만 비인용 공식 보조근거를 더한다. 직접 인용한 모든 근거와 해당 뉴스 장의 공식 반증 자료는 유지한다. 새 경계 시험은 변경 전 2개 실패·8개 통과에서 변경 후 10개 통과였고, 당시 composer 전체 1,254개도 통과했다. 총괄이 시험 함수 이름 8개만 영문으로 정리한 사실과 이름 외 AST 불변은 별도 영수증에 남겼다.

같은 판정의 수치 배열에서 같은 출처의 정확한 원문을 반복할 때, 뒷행이 앞의 직접 원문 행을 `원문참조`로 가리키도록 했다. 참조는 1부터 시작하는 같은 배열의 앞 행에만 적용하며 연쇄·미래·다른 출처·원문과 참조의 동시 지정은 거절한다. 복원한 원문은 기존 항목·값·부호·단위·기간·자기 출처 검증을 그대로 거친다. 최초 구현은 새 시험 22개와 당시 composer 전체 1,276개가 통과했다. 고정된 3개년도 예시의 JSON은 835→617바이트였으며 공급자 토큰 절감을 실측한 수치는 아니다.

독립 검토가 누락했던 빈 출처 ID 조건은 총괄이 추가로 발견했다. 빈 문자열이나 공백뿐인 ID가 같은 경우 참조가 복원됐으며, 비정상 sources 사전에 그 키까지 넣은 오프라인 재현에서는 기존 검증도 통과했다. 참조 형식의 ID가 비어 있지 않아야 한다는 조건을 보완하고 정상 후보 보존을 확인한다. 실제 서비스에서 빈 출처로 보고서가 생성된 사례를 관측했다는 뜻은 아니다. 최초 교차 검토의 ‘반례 없음’과 시험 17개라는 표현은 후속 정정 대상으로 남긴다.

도식의 대조 설명은 인용 ID를 포함해 16자 이내로 요청하고 JSON 바깥의 불필요한 들여쓰기·줄바꿈을 줄인다. 정확 원문 문자열이나 필수 수치·추세·시점 근거는 생략하지 않는다. 관련 도식 36개 검사가 통과했다. 본문 출력 배정은 12,000→16,000, 도식은 2,048→4,096으로 조정하되 작성 4,000·Haiku·본조사 1,000원·건당 2,000원·18회 호출 제한을 유지한다. 출력 배정 확대는 잘린 첫 답변에 여유를 주는 실험이며 재요청 예약을 보장하지 않는다.

앞 SM 비용 감사의 233.63원은 실제 정산액이다. 입력 예약에는 4,096토큰 안전 여유가 더해지고 재요청에는 추가 안내문도 붙으므로 예약액과 같다고 할 수 없다. 이전 예약표·오타 692.91원은 원문을 보존하고 정정 문서로 바로잡았다. 새 배정 비교의 안내문 30~80토큰도 가정일 뿐 실측 범위가 아니다. 현재 맥락 축소와 원문참조의 실제 효과를 아직 측정하지 못했으므로 미래 비용·완료 여부는 다음 실행에서 확인한다.

뉴스는 같은 기사 주소 여부와 같은 사건 내용 여부를 구별해 중복 선택을 보완 중이다. 겹치는 ATM 통계는 풍부한 기존 원문을 보존하는 방향으로 확인하며, 기업당 한도와 전체 프로그램 총액 등 고유 추가 사실은 합치지 않는다. 이 절의 수정 후 통합 검사·새 유료 실행 결과는 아직 없으며 캠페인 확정 사용액은 **7,090.22원**, 잔여 **12,909.78원**이다. 다음 서버 전달기는 과거 금액이 들어 있는 원본을 보존한 `credential_bridge_remaining.py`를 사용하고 명시한 남은 예산을 전달한다.

### 후속 수정 마감

빈 출처 ID 보완 후 해당 수치 참조 시험은 **28개 통과**했다. 최초 교차 검토의 시험 개수와 누락 판단은 별도 정정했다. 재현 스크립트의 임포트·모듈 캐시 오류와 같은 출력 경로 덮어쓰기 이력도 `correction-repro-history.json`에 남겼으며, 그 과정을 성공 한 번으로 축약하지 않는다.

뉴스의 첫 추가 규칙인 숫자 부분집합·0.90 포함유사도는 부정 표현과 다른 회사명까지 합쳤으므로 채택하지 않았다. 실패 구현·검사·문서를 별도 스냅샷으로 보존했다. 최종 추가 경로는 같은 출판사·정확 발행일·주제·주장 종류·시점 상태·사건일에서 짧은 인용의 모든 문장이 긴 인용에 순서대로 대응하는 경우만 합친다. 숫자 뒤 `대였다.`와 `대로 집계됐다.`의 종결형만 비교용으로 같게 보고, 비수치 단어·부정·대상·단위·기간은 그대로 비교한다. 풍부한 원래 인용 객체를 남겨 원문·URL·위치를 바꾸지 않는다. 숫자 배열이 같은 기존 경로의 순서 비교와 대표 선택은 유지했다. 공백 출판사는 이미 입력 모델이 거절하므로 추가 가드를 만들지 않았다.

총괄의 초기 인라인 Python 반례는 PowerShell stdin의 한글 인코딩을 입증하지 못했고, 후속 출력에서는 실제 물음표 변환도 관측했다. 그 두 영수증은 보존하되 반례 증거에서 제외했다. UTF-8 파일 직접 실행인 `root_encoding_recheck.py`로 보존된 실패 구현의 부정·다른 회사 오인 병합과 수정본의 차단을 다시 확인했으며 `root-encoding-recheck.json`을 유효한 관측으로 쓴다. 실제 수집 신원의 읽기 전용 감사는 출판사·날짜·장·정확 원문만 확인했으며 주제·주장 종류·시점 상태·사건일·span은 조사한 보존 자료에서 복원하지 못했다. 테스트의 해당 값은 명시한 모의 입력이므로 과거 실행 전체의 재생이나 새 실제 중복 제거 성공으로 주장하지 않는다.

비용 배정 작업자의 넓은 검사에서 나온 재판정 E2E 실패 2건은 `4df4db49`부터 존재하던 시험용 provider의 라우팅 충돌이었다. 장 안내가 포함됐다는 이유만으로 검수 요청에도 작가 JSON을 반환했다. 시험의 부모 헤더 판별을 먼저 거치게 고쳤고, 사용하지 않는 import를 제거한 최종 파일 **5개 검사 통과**를 확인했다. 생산 검수나 주장·근거·예산 단언은 완화하지 않았다. 비교용 임시 detached checkout은 작업자가 제거했고 총괄의 worktree 목록에서도 잔여가 없었다. 기존 도식 E2E 시험의 낡은 상수 단언 한 줄도 승인된 4,096으로 갱신했다.

최종 영향 검사 16경로에서 **2,343개 통과·24개 기존 경고**, 1,221파일과 HEAD의 전후 불변을 확인했다. 이후 숫자+단위 종결 조건 한 곳과 그 시험만 보완해 뉴스 전체 **409개 통과**를 확인했다. 두 영수증 사이 변경 파일은 `news_intake/grounded_mapping.py`와 그 시험 두 개뿐이며 다른 **1,219파일**은 동일하고 현재 1,221파일 모두 마지막 영수증의 SHA와 같다. 두 결과를 하나의 전체 실행 통과 수로 합산하지 않는다. 근거는 `resume-20260909-compact-final/receipt.json`, `resume-20260909-compact-guards-final/{receipt,root-validation-reconciliation}.json`이다. 새 보고서의 실제 의미·재무·뉴스·시각 품질 판정은 다음 실행으로 남는다.

## 압축 검수 후 실제 평가와 다음 수정

위 변경 15파일을 `2fdd38fbac4d3752f55bb112f41c9341f55d0096`에 저장한 뒤 깨끗한 소스와 마지막 검사 1,221파일 SHA를 확인했다. 평가 폴더는 `app/.local_evaluation_runs/20260909_223412_f9b4a121ca2f9c6f35827b29`, 설정 SHA는 `23af7ac393e332e8cf9ab9102b7eb09f7b7bdb636317fd23513987293a483b4c`다. Haiku·작성 4,000·본문 검수 16,000·도식 4,096과 기존 금액·18회 보호를 유지했다.

| 사례 | batch / run | 실제 결과 | 확정 비용 |
|---|---|---|---:|
| SM | `resume_20260909_compact_SM` / `dd884aa5cf9eb6d15a4177f3f0d5a1f3` | 514.9초·10쪽·18회·의미 수정 필요 | 823.20원 |
| WOORI | `resume_20260909_compact_WOORI` / `efd269f12adf72a011489e9970152358` | 420.1초·11쪽·17회·의미 수정 필요 | 842.13원 |

두 실행의 공급자 호출은 모두 HTTP 200·확정 비용이며 예약·미확정 부채는 0원이다. 캠페인 누적 확정 비용은 **8,755.55원**, 남은 예상 지출 한도는 **11,244.45원**이다. 이 금액은 제공자 지갑 잔액을 조회한 값이 아니다. SM에는 외부 호출 외에 `transition-wrapper / phase-summary` 기록이 하나 더 있으며 확정 비용은 `1.1368683772161603e-13`원이다. 바깥 usage 합계와 개별 비용 합계의 부동소수점 차액을 기록한 것으로, 실제 API 19회가 아니다. 처음 정산 도구는 모든 원장 기록에 HTTP 200을 요구해 실패했고, 다음 판도 차액이 정확히 0이라고 가정해 실패했다. 두 실패 스크립트·관측을 보존한 별도 정산기는 전환 기록의 신원·상태·예약·부채와 `1e-10`원 이하 잔차를 확인하고 원시 비용과 소수 둘째 자리 금액을 함께 남겼다. 정산을 위해 DB를 수정하지 않았다.

이번에는 기록된 v2 응답에 `max_tokens` 종료가 없었다. SM은 작성 9회·본문 계열 검수 3회·도식 1회, WOORI는 작성 10회·검수 2회·도식 1회가 모두 `end_turn`이었다. 뉴스 호출은 각각 5회·4회다. 원시 응답에는 출력 상한도 실제 기록되지만 요청 식별자가 없어 원장 개별 행과 순서만으로 대응시키지 않는다. SM 검수 최대 실제 출력은 14,028토큰, WOORI는 11,440토큰이었다. 상한·실제 사용량·예약금은 서로 구별한다.

**SM 재무**는 해당 실행이 보존한 공식 API와 XML 접수 `20260317000781`로 검증했다. 연결 연간 9칸과 누적 증감률 22.25%·61.32%·334.63%가 일치하고, 2024 순이익 818,849,820원은 8억원의 작은 양수다. 인용 9의 `disclosed_at=2026-03-17`, `collected_at`은 빈 값이다. 작업자가 없는 `document_date`를 읽어 날짜 미확인이라 한 결론은 별도 정정했다. 본문에는 판매조직 인용 32·34로 기획·제작을 설명한 도식, 문화 장의 순수 기대신용손실 인식·측정 정책, 직접 근거 없는 수수료·신규 광고주 확보, 현재 사실을 성장 계획 표에 넣은 설명이 남았다. 이사회 감독·재무부서 담당·위험관리 절차와 실제 여행 전속계약은 정상 근거로 보존한다. 총괄이 PDF 7쪽 머리말만 보고 산업 설명을 문화 장으로 분류한 추정도 철회했다. 실제 저장 위치는 `competitive_position`이며, 문제는 산업 설명에 당사 대응 행동을 덧붙인 것과 장의 범위다. 회사 공시 내용을 회사 설명이라고 귀속한 것 자체는 결함이 아니다.

**WOORI 재무**는 해당 실행의 접수 `20260313000815`와 보존 API로 영업이익·순이익 6칸, 누적 증감률 6.14%·2.66%가 일치했다. 매출 열은 없어 미확인으로 남겼으며 다른 지표로 채우지 않았다. 인용 7의 공시일 2026-03-13이 실제 전달됐다. 본문에는 신용비용 부담의 양보 관계를 이익 감소의 주요 배경으로 바꾼 설명, 출처 85에 없는 ‘금융권 최초’, 신탁 수수료를 퇴직연금에 옮긴 설명과 내용 없는 목차 인용이 남았다. 원비즈 자체개발·러시아 법인 명시·상품별 보증 조건·계획 어법·실제 조직 절차는 유지됐다. 작업자 W-5의 ‘부문은 특정 조건의 범용 대출상품을 제공한다’라는 문장은 조건을 해당 상품에 붙인 것으로 읽을 수 있으므로 부문 전체로 조건을 확장한 결함으로 확정하지 않는다.

SM 뉴스는 선택 인용 6개·고유 URL 5개를 공개 원문과 대조했다. 244·247에는 공개 HTML의 명시적 시세 컴포넌트가 보고서에 섞이지 않았다. 정규식 삭제 대조만으로 구현 실행을 입증한 것처럼 쓴 표현을 좁혔고, 이후 보존 HTML을 실제 `body_boundary()`로 한 번 검사해 DOM 제외 경로를 확인했다. 중간 공백까지 보고서와 완전히 같아지는 전체 경로는 이 재생에서 입증하지 않았다. WOORI 뉴스는 3개 URL이며 ATM 통계 기사는 하나만 선택됐다. 이전 두 기사의 실제 입력 신원을 전부 복원한 비교가 아니므로 이번 선택만으로 중복 제거 수정의 인과 효과를 확정하지 않는다. 프로그램 선정과 총 지원액은 보완 정보이며, 이번 선택 인용 326에 개별 300억원 숫자가 없다는 점도 구별한다. 두 사례의 접속 제한·수집 상한·분석 미확인을 자료 부재로 바꾸지 않았다.

저장 JSON·DB 왕복·메모리 PDF 재출력·전체 쪽 PNG SHA가 각 사례에서 일치하고, Claude는 SM 10쪽·WOORI 11쪽을 직접 확인했다. 총괄은 SM 3·7쪽을 추가 확인했다. 잘림·겹침은 관측하지 않았으나 쪽 머리말의 장 이름은 별도 조사 대상으로 남겼다. 요청은 `REPORT_RELEASE_MODE=FULL`이고 실제 결과는 봉인되지 않은 legacy 부분 결과다. 작업자의 ‘FULL 요청 자체가 없었다’는 서술은 원본을 보존한 정정 파일로 철회했다. PDF 바이트 일치를 FULL 승인으로 취급하지 않는다.

정산·검수 후 서버를 종료했다. 첫 종료 시도는 JSON 날짜가 `DateTime`으로 자동 변환되는 점을 놓쳐 신원 사전검사에서 중단됐고, 보완 시도도 종료 순회 중 신원검사에서 멈췄다. 두 실패를 보존했으며 별도 CIM 조회에서 소유 PID 9908·32048·31620·3120·38240과 8020 listener 전부 부재, 사용자 Orca 27396 생존을 확인했다. 현재 품질 합격은 **0/8**이다. 다음에는 Claude 3명이 문화 장의 순수 회계정책 경계, PDF 머리말, 인과·자기선언·도식의 직접 근거 결속을 나누어 수정하고 총괄이 연결·교차 검증한다. 푸시·배포·설치 변경은 하지 않았다.

증거: `resume-20260909-evaluation-launch/{settled-compact-SM,settled-compact-WOORI,stopped-compact}.json`, `resume-20260909-{SM,WOORI}-compact-{semantics,news,quality}/`. 각 폴더의 `correction.json`을 최초 보고와 함께 읽는다. SM 의미 정정 파일의 결함 개수 필드는 서로 모순되므로 총괄은 개수를 성과 지표로 합산하지 않고 개별 결함과 정정된 범위를 사용한다.

### 같은 날 후속 수정·독립 검토

PDF 이어짐 머리말 수정은 `91bbb3e7e65c98c8a64e81eab9deebe08a436172`로 저장했다. 한 쪽에 여러 장이 시작하면 그 쪽의 첫 장 표시를 유지하고, 다음 쪽에는 마지막으로 시작한 장을 물려준다. SM 10쪽 전체 텍스트 비교에서 7쪽 머리말 한 줄만 바뀌었고, 총괄도 렌더된 7쪽을 확인했다. 기존 v1 데모의 93,519바이트·SHA는 그대로다. 관련 216개 시험 통과와 최종 4개 파일 SHA 일치를 확인했다. WOORI 11쪽은 기존 저장 자료의 쪽별 제목을 대조해 같은 오류가 없음을 확인한 것이며, 이 후속 검토에서 새 코드로 WOORI PDF를 재생한 것은 아니다. 증거는 `resume-20260909-pdf-running-header-fix/`다.

문화 장의 순수 회계 인식·측정 문단을 제한하는 가드는 실제 `culture` 장의 본문에만 연결했다. 요약이나 다른 장의 오래된 주장 슬롯에는 적용하지 않는다. 독립 검토에서 실제 SM 문단 7개 중 기대신용손실 계산 문단만 제한되고 조직·위험관리 절차 6개는 유지됐다. 같은 절의 무관한 검토 동사와 미래 예정형이 필터를 지나가는 한계도 남겼다. 이는 장 적합성의 좁은 필터이며 출처와 조직 절차의 의미 결속까지 증명하지 않는다. 증거는 `resume-20260909-culture-accounting-correction/`와 `resume-20260909-culture-independent-review/`다.

인과·회사 직접표현 보조 검수는 본문·요약·독립 도식이 공통으로 부르는 `constrain_verdicts`에 연결했다. 최초 통합 시험 6개에서 기존 파서가 새 `관계` 키를 알 수 없는 필드로 거절하는 문제를 확인하여 관계 배열의 형태 확인과 의미 확인을 분리했다. 각 후보의 추가 검증 안내에도 관계 항목을 표시한다. 기존 정상 인과 보존 시험의 가짜 응답이 관계 근거를 내지 않던 2개 실패는 실제 원인·결과·인용문을 응답에 추가해 바로잡았으며, 관련 통합·기존 본문 검사 46개가 통과했다. 이는 모델의 실제 판단 품질을 입증한 시험이 아니다.

독립 검토는 인과 슬롯을 ‘감소/증가’로 축소하는 우회, 두 인과 주장 중 하나만 근거를 내는 누락, ‘원인이라고 보기는 어렵다’라는 완곡 부정, 원문 사전에 빈 키가 실제 있는 경우의 빈 근거 id 허용을 추가로 재현했다. `resume-20260909-direct-support-independent/`에 실패를 보존했으며 후속 보완과 별도 회귀시험을 진행 중이다. 역할·과금의 단일 대상 문자열 설계는 약한 필드와 무관한 상품 근거로 통과할 수 있어 채택하지 않았다. 그 설계 실험의 최초 13/15·14/15 JSON은 작업 중 덮어써져 보존되지 않았고, 현재 15/15만 남았다는 위반과 한계를 `resume-20260909-flow-direct-support-design/06-correction-and-remaining-risks.md`에 기록했다. 현재 채택한 역할·과금 변경은 같은 상품·주체의 역할·대가·반복수익을 각각 대조하도록 작성·검수 지시를 강화한 것이며 실제 모델 효과는 재평가 전까지 미확인이다.

이 후속 수정 중에는 새 유료 평가를 하지 않았다. 확정 누적 8,755.55원·잔여 11,244.45원이고 품질 합격은 여전히 0/8이다. 다음 서버는 새로 동결·검사·커밋한 코드와 현재 잔여 예산으로 시작해야 한다. 최종 검사 준비의 `resume-20260909-relations-final/test-selection.json`은 PowerShell 중첩 배열 오류가 난 산출물로 보존했으며, 실제 사용할 경로는 검증한 18개 항목의 `test-selection-flat.json`이다. API 자격증명 파일은 기존 원본 작업 폴더의 `analysis_engine/.env`이며 값은 출력하거나 복제하지 않았다.

### 9월 10일 인과 보완 적용과 FAQ 원문 복원

총괄은 인과 보완 후보 세 파일을 이전 SHA와 대조한 뒤 실제 코드에 적용했다. 원인·결과를 방향 낱말만으로 채운 관계, 증명하지 않은 두 번째 인과, 완곡하게 부정한 원문, 빈 근거 id를 제한한다. 정상 부정 후보는 긍정 인과 검사 대상에서 제외하고, 같은 인과를 두 인용이 뒷받침하는 경우는 보존한다. ‘가장 큰 원인’·‘주요 배경’·‘주요 요인’도 후보 표지와 원문 역할 틀을 맞췄다. 적용 영수증은 `resume-20260909-causal-coverage-patch/root-application-receipt.json`이다. Opus의 격리 스텁 44개 통과와 별개로, Sonnet이 실제 앱에서 독립 시험 12개와 구현 시험 44개를 한 번 실행해 **56개 통과**, 관련 네 파일의 전후 SHA 일치를 확인했다. 근거는 `resume-20260910-causal-independent-green/`이다. 닫힌 문법 밖의 의미, 구두점 없는 다른 절의 부정, 원인구 자체의 부정 낱말 범위까지 완전하게 판별하는 일반 의미 엔진은 아니다.

SM 채용 FAQ는 robots 확인 후 대상 페이지 한 번의 공개 GET으로 복원했다. 기존 추출 방식으로 계산한 문서 SHA `4c36e6c3166cf5c1c52a2c27b7cf5dd28f2503b0ba28082c88060e2b95d969ef`와 인용 22의 조각 SHA `92bf794b647f74cef3ccf19030e31c4507a2353d99328ee3717e74da3ff8d02c`가 기존 실행 메타데이터와 일치했다. 같은 원문 내용의 복원이며 과거 HTTP 응답 자체를 보관했다는 뜻은 아니다. 원문은 파트타이머·인턴의 경력 기간 산정에 어려움이 있다는 원칙, 프리랜서의 직무 연관성 판단, 다양한 경험 참고와 상황별 예외를 구별한다. 기존 보고서가 직무 연관성 조건을 세 유형 모두에 붙인 부분은 지지가 부족하다. 이를 계기로 문화 작성과 공통 본문·요약 검수에 대상별 인정·불인정·예외·참고를 보존하는 일반 규칙을 추가했다. 관련 59개 시험은 문구와 기존 코드 계약을 확인한 결과이며 실제 모델의 개선 효과는 후속 생성으로 확인한다.

FAQ 감사의 최초 `checked_at=2026-09-10T00:00:00+09:00`은 스크립트가 넣은 고정 자정으로 실제 요청 시각 증거가 아니다. 원본 일곱 파일을 보존한 `resume-20260910-SM-faq-current-reconstruction/time-and-hash-correction.json`에서 이를 정정하고 두 해시를 저장된 추출 문자열로 다시 계산했다. 파일 시각과 완료 통지는 요청 시각으로 바꾸지 않았다.

최종 영향 18경로 첫 실행은 **2,755개 통과·1개 실패·24개 경고**, 소스·시험·템플릿 1,230파일과 HEAD가 전후 동일했다. 실패는 도식 시험이 전체 프롬프트에서 처음 나온 ‘결과’ 키를 검색하여, 새 인과 관계의 결과 칸을 판정 결과 칸으로 혼동한 단언이다. 실제 판정 응답 형식의 순서를 검사하도록 해당 시험 한 파일만 보완 중이며 생산 코드는 동결했다. `resume-20260909-relations-final/`의 실패 로그·XML·영수증은 보존한다. 앞선 Python 인라인 실행은 PowerShell의 인용부호 전달에서 구문 오류가 나 시험 자체를 시작하지 못했고, 오류 관측 기록 후 UTF-8 실행 파일로 위 18경로를 실행했다. 아직 새 유료 호출·푸시·배포·설치 변경은 하지 않았다.

도식 응답 순서 시험은 실제 생성 프롬프트의 판정 JSON 예시 줄을 정확히 하나 찾고, 그 안에서 대조근거가 판정 결과보다 앞서는지 비교하도록 보완했다. 해당 파일은 수정 전 1개 실패·8개 통과, 수정 후 9개 통과가 보고됐다. 작업자가 저장한 수정 전 로그는 원시 출력 전체가 아니라 실행 때 읽은 일부를 옮긴 관측 기록이며, 원시 실패 근거는 총괄의 앞선 통합 `app.log`에 남아 있다. 관련 증거는 `resume-20260910-diagram-schema-test-fix/`다. 이 후속 시험이 전체 1,230파일의 불변을 다시 증명했다고 표현하지 않는다.

별도 읽기 검토에서 도식 칸의 결합자와 인과 역할 틀의 절 경계가 충돌하는 문제를 발견했다. 실제 두 검수 경로를 모의 응답으로 호출하면 한 칸의 완전한 인과 설명은 남고, 원인과 술어를 두 칸에 나눈 동일 설명은 `causal_relation_claim_not_covered`로 빠졌다. 최초 운영 경로표 fixture는 표 머리말의 의미까지 온전히 지지하는 실제 콘텐츠 양성 증거가 아니므로 기계 결합 오류의 재현으로만 사용한다. 실제 제품 이름과 제품의 역할을 모두 원문이 지지하는 포트폴리오 표 형태로 양성을 보완하고, 도식 구조를 별도로 전달하되 일반 산문의 절 경계를 완화하지 않는 수정을 진행한다. 해당 생산 파일에 한해 동결을 풀었으며, 최종 검사는 새 `resume-20260910-split-flow-final/`에서 실행할 준비를 했다. 이전 통합 검사의 통과를 변경된 코드에 그대로 이월하지 않는다.

도식 보완은 후보 문자열을 유지하고 실제 칸 배열을 별도 전달하는 방식으로 적용했다. 원문·자기 인용·방향·부정 대조는 유지하며 후보의 원인 칸과 결과 술어 칸만 제한적으로 연결한다. Opus 관련 1,495개 통과 후 독립 7개와 총괄의 제품 표 4개도 각각 통과했지만, 총괄이 실제 포트폴리오 머리말을 재대조하니 역할은 두 번째 ‘범위’ 칸이 아니라 네 번째 ‘사업적 역할’ 칸이었다. 앞선 양성 입력을 완성된 제품 표 적합성까지 증명한 것처럼 취급한 해석은 철회한다. 실제 네 번째 칸으로 옮겨 재실행한 두 분할 입력은 실패하고 두 완전 문장 입력은 통과했으며 `resume-20260910-empty-flow-cells/`에 원본 코드·시험과 실패 영수증을 보존했다.

총괄은 내용 없는 중간 칸만 건너뛰도록 인접성 판정을 보완했다. 칸 배열과 후보 문자열의 일치·원래 오프셋은 유지하고, 내용 있는 다른 칸을 넘어 묶지는 않는다. 일반 산문에도 이 구조 근거를 제공하지 않는다. 두 독립 제품 표 입력도 역할을 실제 네 번째 칸에 배치했다. 관련 29개 중 행동 검사 28개는 통과하고 안내 문구의 낡은 ‘떨어진 칸’ 단언 하나가 실패하여, 실제 규칙인 ‘빈 칸을 뺀’ 인접성 및 ‘내용 있는 다른 칸을 건너뛰지 않음’을 확인하도록 단언을 수정했다. 이 실패 영수증은 `resume-20260910-empty-flow-cells-green/`에 남겼다. 이후 모든 코드·시험을 고정하고 준비된 18경로 전체 검사를 시작했다. 새 유료 생성은 아직 실행하지 않았다.

최종 18경로 전체 검사는 **2,785개 통과·24개 기존 경고·215.90초**로 끝났다. 소스·시험·템플릿 **1,233개**와 HEAD가 전후 동일하다. 이는 `resume-20260910-split-flow-final/receipt.json`의 단일 전체 실행 결과이며 앞선 부분 검사 수를 합산한 값이 아니다. 실제 제품 표의 네 번째 역할 칸·빈 중간 칸 보존, 다른 내용이 있는 칸을 넘지 않는 경계, 인과·수치·기간·뉴스·비용·저장·PDF 회귀를 함께 포함한다. 다음에는 이 소스를 저장한 커밋과 현재 잔여 11,244.45원으로 새 평가 서버를 시작한다. 실제 8사례의 품질 판정은 계속 별도이며 아직 0/8이다.
