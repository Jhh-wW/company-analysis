# 2026-09-09 보고서 품질 개선 재개

이전 중단 커밋 `e3d6762d`에서 작업을 재개했다. 포트폴리오 집계는 이번 범위에서 제외하고 기업 규모·산업별 보고서 품질과 하위 작업자 알림 개선을 진행한다. 이전 원본 세션과 트랜스크립트는 변경하지 않았다.

## 반영한 코드

뉴스 분석의 공급자 실패 진단에 허용된 API 오류 유형과 고정 사유 코드만 남긴다. 공식 문서로 확인한 스키마 복잡도 및 조직·워크스페이스 지출 한도 신호를 구분하며 원문 응답·키·프롬프트는 저장하지 않는다. 기존 요청, 비용 정산, 재시도 정책은 바꾸지 않았다.

- 변경: `app/src/features/pipeline/constants.py`, `real.py`, 신규 `provider_error_diagnostics.py`, feature 내부 테스트.
- 기존 회귀 67개와 신규 12개: **79개 통과, 하위 검사 10개 통과**.
- 현재 SDK 0.122.0을 연결한 모의 HTTP 검사에서 요청의 명백한 미지원 스키마 전송이나 도구 충돌은 재현되지 않았다. 당시 HTTP 400의 상세 오류 본문은 없어 정확한 원인은 미확정이다.

## 전체 검증

- 첫 전체 실행: 8,978개 통과, 30개 실패, 1개 건너뜀. 생산 Python 413개와 HEAD는 실행 전후 동일했다.
- 짧은 새 임시 경로에서 실패가 있던 9개 파일을 재실행: **194개 통과, 실패 0개, 1개 건너뜀**. 해당 검증자의 코드 수정 없이 실패가 사라졌다. 긴 Windows 임시 경로가 관련된 검증 환경 문제였으며 보호 코드는 완화하지 않았다.
- 신규 진단 포함 전체 실행: **9,020개 통과, 실패 0개, 1개 건너뜀, 하위 검사 10개 통과**. 다만 검사 도중 다른 작업의 회사 검색 수정으로 생산 Python이 414개에서 417개가 되어 고정된 수정본의 검증으로 인정하지 않았다.
- 별도 Orca 작업공간 `report-quality-20260909`와 브랜치 `Jhh-wW/report-quality-20260909`를 기존 커밋에서 만들고 진단 변경 4파일만 옮겼다. 기존 작업공간의 회사 검색 변경은 그대로 보존한다. 이 별도 작업공간의 전체 검사를 완료한 뒤 실제 평가를 진행한다.

## 이전 실패 요청 비용의 대사

기존 94.41원은 실제 청구액이 아니라 미확정 보수부채였다. HTTP 400만으로 0원 처리하지 않고 다음 외부 근거를 추가 확인했다.

1. [Anthropic 공식 과금 안내](https://support.claude.com/en/articles/8977456-how-do-i-pay-for-my-claude-api-usage)의 실패 요청 비과금 정책. 성공 처리 중 연결 종료·시간 초과 예외와 구분했다.
2. 기존 성공 요청 15개의 식별자 해시가 공식 Console의 청구 로그와 모두 일치해 같은 서비스 계정을 확인했다.
3. 공식 Console의 2026-09-08 전체 시간대 사용량은 03·06·09 UTC에만 존재했다. 대상 실패 시각 10:48:59 UTC가 포함된 10 UTC에는 사용량이 없었다.
4. 내부에는 단일 요청의 `RESPONSE_RECEIVED`, HTTP 400, `BadRequestError`가 보존돼 있었다.

별도 작업자가 위 증거 결합을 검토하고 메모리 복제본에서 기존 대사 함수와 평가기의 비용 읽기를 검증했다. 총괄은 **2026-09-09 13:12:09 KST**에 원본 백업과 증거 해시를 보존한 뒤 기존 `state_machine.resolve_liability(CONFIRM_ZERO)`를 로컬 유지보수로 실행했다. 주체 `codex-run-f865a412edb2`, 사유 `provider-zero-policy-usage-20260909`의 감사 사건 1개만 추가했다. 과거 사건과 다른 테이블은 모두 불변이다.

대사 트랜잭션 확정 후 읽기 전용 검증에서 `KNOWN_ZERO`, 부채 0원, `billing_uncertain=false`와 비용 요약 정합성을 확인했다. 보고서 생성 실패라는 결과는 그대로다. 과거 체크포인트와 `ledger.json`은 당시 스냅샷으로 보존했다. 공급자가 발행한 요청별 청구 확인서를 받았다는 뜻은 아니다.

기존 평가 누적 확정 비용은 **712.57원**, 남은 미확정 부채는 **0원**이다. 기존 20,000원 평가 예산에서 남은 예상비용 한도는 19,287.43원이다. 건당 예상 예약 한도 2,000원과 함께 후속 평가에 적용한다.

사용자가 API 크레딧을 직접 충전했고 2026-09-09 13:00 KST 무렵 공식 Console에서 가용 잔액 19.92 USD를 확인했다. 이 충전은 과거 요청의 비과금 판정과 별개다.

## 실제 평가와 알림

확정 manifest의 우리은행·SM·HYBE·YG·JYP·멀티캠퍼스·앱솔브랩·인텍에프에이 8사를 평가한다. 앞선 운영 관측 스위치 `ProductionObserved20260908`를 사용하되 현재 운영 배포와 완전히 같다고 주장하지 않는다. 실제 최종 품질 합격은 아직 **0/8**이며, 자동검사 통과를 보고서 품질 합격으로 대체하지 않는다.

Orca 1.4.197 공개 소스에 Codex 하위 작업자의 완료 알림만 거르는 패치를 준비했고 관련 107개 검사가 통과했다. 별도 Windows 빌드 후보를 준비 중이다. 현재 설치판에는 적용하지 않았으며 같은 PTY의 독립 사용자 작업 재사용, 원격 실행과 공식 업데이트 이후 지속성의 한계가 남아 있다. 메인 Codex·Claude 알림 및 작업자 상태보고를 보존하는 것이 요구사항이다.

이전 실행과 비용 대사의 상세 증거는 원래 작업공간 `C:/Users/jh-wo/.claude/workspace/기업분석2`의 Git에서 제외된 `.local-artifacts/resume-20260909-*`에 있다. 특히 `provider-console-audit.json`, `provider-account-match.json`, `settlement-review.md`, `settlement/execution.json`, `settlement/post-commit-ledger.json`에 비용 판단과 검증을 보존했다. 이후 고정된 작업공간의 전체 검사·실제 8사 평가·알림 설치 결과는 진행에 따라 갱신한다.
