# 2026-09-10 야간 보고서 품질 검증

## 인과·도식·문화 범위 수정

`a6cde011b629f2e66fca6c61a06f0530cbae8da7`에 자기 인용의 인과관계, 도식의 칸 사이 관계, 문화 장의 회계 정책 범위 수정과 관련 시험을 저장했다. 직전 PDF 머리말 수정은 `91bbb3e7e65c98c8a64e81eab9deebe08a436172`이다.

18개 경로를 실행한 최종 회귀 시험은 **2,785 통과, 경고 24, 215.90초**였다. 시험 전후 소스·테스트·템플릿 1,233개가 같았으며, 커밋 뒤 실제 평가와 정산 전에도 해시 일치를 확인했다. 영수증은 `.local-artifacts/resume-20260910-split-flow-final/receipt.json`에 있다. 이 수치는 한 번의 최종 실행 결과이며 앞선 부분 시험과 합산하지 않는다.

## 새 SM 실행과 공급자 오류

- 평가 루트: `app/.local_evaluation_runs/20260910_010717_c238d6f814f4edde84135d57`
- 배치: `resume_20260910_relations_SM`
- 실행: `dff14f2a1957419e50b8abe81cb18a27`
- 생성 소스: 위 `a6cde011…`
- 설정 SHA-256: `c35183cf5415abc96ba73488e48850f990d7a1702c06d48c896d66da5c0dd4cb`

실제 생성 실행은 한 번 접수했다. 뉴스 근거 확인 중 공급자 요청 두 건 가운데 첫 요청은 성공해 37.14원의 사용량이 기록됐고, 두 번째 요청은 Anthropic HTTP 500 `InternalServerError`로 끝났다. **보고서와 PDF는 만들어지지 않았다.** 이 실행을 품질 통과로 세지 않는다.

실패 요청은 `req_011CetA4qfRZ1ZHDhQ5BTsTP`, 비용 시도는 `attempt:84ed8723101d491ebccbe62bb860735a`이다. 처음에는 전송 전 추정치 56.59원이 보수부채로 남아 다음 실행을 막았다. 56.59원은 실제 청구액이 아니며, 내부 원가 요약 37.14원만으로 정산 완료를 주장할 수 없었다. 두 요청이 동일 내용의 중복이나 재시도였다는 근거도 없으므로 해당 표현은 철회했다.

## 공식 정책에 근거한 로컬 정산

[Anthropic 공식 결제 안내](https://support.claude.com/en/articles/8977456-how-do-i-pay-for-my-claude-api-usage)는 실패 요청을 청구하지 않는다고 설명한다. 성공할 예정이던 요청에서 클라이언트 연결이 끊기거나 시간 초과가 발생한 경우는 예외다. [공식 오류 설명](https://platform.claude.com/docs/en/api/errors)은 HTTP 500을 공급자 내부 오류로 구분한다.

2026-09-09 16:35:39~40 UTC에 공식 결제 페이지를 직접 GET해 HTTP 200 원문과 두 조항을 보존했다. 원문 SHA-256은 `0ba284464c06427a211495788ce66a73cd325c4deb9b511585435fabe65283d5`이다. 실제 호출 경로의 SDK 자동 재시도 0, 비스트리밍 전송, 공급자 요청 ID가 붙은 HTTP 500 응답을 Opus가 별도로 확인해 연결 끊김·시간 초과 예외를 배제했다.

원본 DB와 실패 산출물을 보존한 뒤, 사본에서 기존 `state_machine.resolve_liability(CONFIRM_ZERO)`를 실행하는 예행을 검증했다. 루트는 2026-09-10 01:42:03 KST에 같은 공개 도메인 API를 지정 로컬 평가 DB에만 적용했다. 주체는 `codex-root:run_f865a412edb2`, 이유는 `provider-zero-confirmed`로 기록했다. 관리자 로그인·CSRF를 위조하지 않았으며, 원래 사건을 직접 SQL로 수정하지 않았다.

독립 사후 검증은 다음을 확인했다.

- 원본 사건 6개가 그대로이고, `KNOWN_ZERO` 사건 1개만 추가됐다.
- HTTP 500, 오류 종류와 공급자 요청 ID가 새 사건에도 보존됐다.
- 변경된 테이블은 사건 원장과 SQLite의 일련번호 표뿐이었다.
- 확정 원가 37.14원은 그대로이고, `billing_uncertain`은 `false`가 됐다.
- 공급자 시도는 여전히 두 건이며, 보고서 없음·고객 청구 0원은 유지됐다.

이는 **공식 정책과 해당 오류 응답을 연결한 미청구 판정**이다. 공급자 청구 원장에서 해당 요청의 0원 항목을 직접 확보한 것은 아니다. 콘솔 집계 사용량이나 지갑 잔액은 이 판정의 근거로 사용하지 않았다.

이후 기존 CLI의 `--resume-only`로 GET 조회만 재개해 체크포인트를 `complete`로 마감했다. 결과는 계속 `생성_실패`, 원가는 37.14원이다. 결과 페이지 HTTP 200과 평가 체크포인트 완료는 보고서 생성 성공을 의미하지 않는다. 평가 서버의 소유 프로세스 5개 및 8020 리스너 부재를 확인했으며 사용자 Orca는 유지했다.

관련 증거:

- `.local-artifacts/resume-20260910-SM-provider-billing-policy/before-reconciliation/`: 원본 DB·설정·실패 증거와 해시
- 같은 폴더의 `official-policy-capture/`: 실제 공식 페이지 응답과 조회 영수증
- 같은 폴더의 `applied-reconciliation/`: 정산 의도·결과 영수증
- `.local-artifacts/resume-20260910-SM-resolution-independent/`: 독립 사후 대조
- `.local-artifacts/resume-20260909-evaluation-launch/stopped-relations.json`: 최종 서버 부재 확인
- 같은 폴더의 `relations-stop-attempt-observations.md`: 종료 도중 인코딩 오류·신원 재검사 오류와 최종 확인의 구분

## 현재 남은 검증

캠페인 한도는 20,000원이다. 앞선 확정 8,755.55원과 이번 37.14원을 합치면 8,792.69원, 잔여는 11,207.31원이다. 이는 보고서 평가 캠페인의 내부 AI 원가이며 Claude Code 작업자 비용이나 Anthropic 계정 지갑 잔액이 아니다. 건당 2,000원, 본조사 1,000원, 생성 호출 최대 18회와 Haiku 모델·출력 한도는 유지한다.

새로운 품질 검증 대상 8개는 SM, 우리은행, HYBE, YG, JYP, 멀티캠퍼스, 앱솔브랩, 인텍에프에이다. 현재 수정본의 실제 보고서 품질 통과는 아직 **0/8**이다. 생성 성공, 실제 공개가 허용된 보고서, 근거와 문장의 품질, 비용 정산을 각각 확인해야 한다.

현재 진행 중인 사실을 미래 계획으로 옮기지 않도록 작성·검수 지침을 보완했다. 해당 대상의 미실행 행동·미래 목표가 자기 인용에 실제로 있어야 하며, 진행 중인 사업에 명시된 향후 단계는 보존한다. 원문 시제를 미래형으로 바꾸거나 없는 날짜를 채우지 않는다.

평가 재개 시 JSON 6종과 PDF를 다시 쓰기 전에 이전 파일을 고유한 사본으로 보존한다. 같은 내용의 이전 사본을 재사용할 때도 디스크 반영을 다시 확인하며, 보존 실패가 계속되면 진행을 막는다. 실패 중 원본 유지, GET만 사용하는 재개, 중간 PDF 오류 뒤 재개, 디스크 오류 복구를 실제 평가 도구 시험으로 확인했다. 비용 판정·상한·모델·유료 호출 경로는 바꾸지 않았다.

후속 수정본의 최종 검증은 **관련 18경로 2,785 통과·경고 24·210.41초**, 별도 **평가 도구 37 통과·경고 1·2.53초**다. 두 실행에서 같은 소스·테스트·템플릿 1,233개의 해시와 HEAD가 유지됐다. 서로 다른 두 실행이며 앞선 부분 시험을 더하지 않는다. 통합 확인 영수증은 `.local-artifacts/resume-20260910-evidence-future-verified/receipt.json`에 있다. Opus의 최종 정적 검토에서도 추가 수정이 필요한 결함은 발견되지 않았다. 실제 보고서의 문장 품질 통과를 이 오프라인 시험으로 대신하지 않는다.

진단 보조 도구도 실패 실행을 읽을 수 있도록 별도 사본을 보완했다. 빈 보고서 ID는 실제 비보고서 결과이고 DB에도 보고서가 없을 때만 허용한다. 새 인과·문화 검수 사유와 공급자 오류 종류를 표시하고, 모르는 값은 여전히 미확인으로 남긴다. 원본 도구를 바꾸지 않은 최종 사본은 `.local-artifacts/resume-20260910-observer-failed-run-final/`에 있다.

평가기 보존 후보를 개발하던 작업자가 임시 `01-run.log`를 덮어쓴 사실은 `.local-artifacts/resume-20260910-evaluator-evidence-ready/00-provenance-correction.json`에 명시했다. 유실된 실행 원문을 재구성하지 않았고, 이후 최종 검증은 새 로그에 직접 캡처했다. 이는 실제 SM 실패 증거의 덮어쓰기와는 별개의 후보 시험 기록 문제다.

푸시·배포·Orca 설치본 교체는 하지 않았다. 원래 작업 폴더의 회사 검색 관련 미커밋 변경과 원본 세션 기록은 건드리지 않았다.
