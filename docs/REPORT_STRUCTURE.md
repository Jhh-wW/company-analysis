# 회사분석 보고서 구조

- 상태: **현재 정본(Canonical)**
- 확정일: 2026-08-23
- 적용 범위: 신규 회사분석 보고서의 조사·작성·검토·출력 구조
- 세부 실행 기준: [`출력물 기준`](./출력물%20기준/README.md) 폴더의 목차별 문서와 공통 QA 규칙
- 프로그램 강제 계약: [`런타임 출고 계약`](./출력물%20기준/90_공통_규칙/런타임_출고_계약.md)

## 최상위 목적

이 보고서는 **기업이 취업준비생에게 자사에 관해 반드시 알아야 한다고 판단하는 정보**를 전달한다.

- 취업준비생이 개인적으로 궁금해하는 연봉·근속연수 등의 정보는 자동으로 포함하지 않는다.
- 직무별 업무·KPI·자기소개서 문장·면접 답변 같은 맞춤 정보는 제공하지 않는다. 회사·직무·채용공고별 변수가 커서 보고서의 일관성과 정확성을 떨어뜨리기 때문이다.
- 회사 공식 자료를 우선하고 외부 분석은 사실 검증과 보완에 사용한다.
- 모든 사실 문장은 확인 가능한 원문 근거와 연결한다.
- 선택 하위 사실은 근거가 없으면 일반론이나 추측으로 채우지 않는다. 1~9장은 모두 기본 보고서의 필수 장이며 장별 최소 계약이 하나라도 부족하면 전체를 `GATE_STOPPED`로 끝낸다. 9장도 예외가 아니어서 안내문만 담은 장으로 대신할 수 없다.
- `핵심 요약`은 검증된 1~9장에서 이미 출고 가능한 문장 3~5개를 결정론적으로 골라 첫 장 하단 고정 영역에 배치한다. 요약 전용 AI가 새 문장을 만들지 않는다.
- 표지 제목은 첫 장 중단에 `[회사명]`과 `분석 보고서`를 두 줄로 쓰며, 제목에서 `기업`을 제외한다.

## 최종 목차와 작성 내용

### 핵심 요약

본문에서 지원자가 반드시 알아야 할 회사 고유 결론 3~5개를 추려 한눈에 보여준다. Writer 검수를 통과한 본문 `FactRecord` 문장을 글자 변경 없이 재사용한다. 같은 장에서는 숫자 없는 문장을 먼저 고르되 검증 문장이 숫자를 포함한 것뿐이면 그 문장을 그대로 허용하며, 요약 전용 AI·Reviewer는 호출하지 않는다.

### 1. 기업 정체성

회사가 공식적으로 정의한 존재 목적과 산업 내 역할을 설명한다.

### 2. 사업 구조와 수익 모델

누구에게 무엇을 제공하고 어떤 방식으로 매출과 이익을 만드는지 설명한다.

필수 소절 `핵심 고객과 우선시장`에서는 다음 공통 열을 사용한다.

`대상 | 역할(구매자·사용자·수혜자) | 제공 가치 | 우선시장과 단계 | 판단 근거·기준시점`

- 고객은 최대 3개로 제한하고 구매자·사용자·수혜자가 다르면 구분한다.
- 시장은 실제 집중 순서에 따라 `핵심·성장·진입`으로 표시한다. 해당 지역 전용 제품·조직·파트너가 확인될 때만 `현지화`를 별도 구분으로 쓸 수 있다.
- 지역 차이가 중요하지 않은 회사는 지역 대신 산업·용도·판매 채널의 우선순위를 쓴다.
- 공식 전략 또는 최근 성과와 실제 투자·출시·유통 확대가 함께 확인될 때만 우선순위를 부여한다.
- 근거 없는 고객 성향, 공개되지 않은 고객사 추정, 우선순위 없는 국가 나열은 금지한다.

### 3. 핵심 제품·서비스와 포트폴리오 역할

회사가 현재 중점적으로 추진한다는 근거가 확인된 제품·서비스를 1~3개 설명하고, 회사의 주요 제품·서비스·브랜드·IP·사업 단위가 전체 포트폴리오에서 맡는 역할을 정리한다.

공통 열은 다음과 같다.

`핵심 자산 | 분석 단계(신규·성장·주력·안정) | 핵심 고객·시장 | 수익 경로 | 포트폴리오 역할·불확실성`

- 콘텐츠 IP뿐 아니라 제조 제품군, SaaS 모듈, 금융상품, 바이오 파이프라인, B2B 솔루션에도 동일하게 적용한다.
- 각 중점 항목은 원문에 실제로 적힌 정확한 `subject_label`, 사업 역할 `product_role`, 서로 다른 실제 우선 신호 `priority_signals` 2개 이상을 가져야 한다. 같은 수치의 표현만 바꿔 두 신호로 세거나 세 개를 억지로 채우지 않는다.
- 같은 역할을 맡는 자산은 묶고 3~6개 역할군만 보여준다.
- 개별 수익이 공개되지 않았으면 추정하지 않는다.
- 표의 공통 자료 범위·기준일·출처는 표 위에 한 번만 적고 각 행에 반복하지 않는다.
- 특정 자산만 자료 시점이나 상태가 다르고 해석에 영향을 줄 때만 해당 행에 예외 근거를 적는다.
- `신규·성장·주력·안정`은 회사 공식 분류가 아니라 보고서의 분석 라벨이다. 따라서 표 머리글에 항상 `분석 단계`라고 표시한다.

### 4. 3개년 주요 변화와 실행 `#과거`

회사의 실제 결산월을 기준으로 비교 가능한 최근 완료 `fiscal_year` 3개와, 기준일 이전 36개월에 완료된 실행(`completed_execution`)을 정리한다. 각 완료 실행은 원문의 실제 사건일 `event_date`를 가지며 변화 해석은 같은 장의 완료 실행·실적 `basis_fact_ids`에 결속한다. 모든 회사를 달력연도 12월 31일로 고정하지 않는다.

### 5. 당면 과제와 대응 `#현재`

아직 해결되지 않은 주요 사업 문제와 회사가 실제로 시작한 대응을 설명한다. 모든 대응의 `response_to_fact_id`는 같은 장의 미해결 문제 `fact_id`를 직접 가리켜야 한다.

### 6. 성장 전략 `#미래`

회사가 공식적으로 밝힌 향후 제품·시장·투자·사업 확장 계획을 정리한다.

### 7. 사업 운영과 파트너 구조

생산·기술·데이터·운영 체계와 주요 파트너·유통·IP가 사업에서 맡는 역할을 설명한다.

### 8. 인재상과 일하는 방식

공식 채용·문화 자료에서 확인되는 회사 공통 가치와 공개된 실제 업무 사례를 정리한다. 이 장은 직무별 업무 안내가 아니라 회사가 공식적으로 강조하는 일하는 기준을 확인하는 장이다.

- `지원자가 보여줄 행동`처럼 회사 근거가 약한 포괄적 행동 지침은 만들지 않는다.
- 서로 다른 조직의 사례를 하나의 전사 공통 업무 절차처럼 합치지 않는다.
- 조직·직무·상황·행동이 확인된 사례만 쓰고 적용 범위를 함께 표시한다.
- 특정 직무의 KPI·필수역량·과제·지원전략을 만들거나 모든 직무를 빠짐없이 다루려 하지 않는다.

### 9. 동종업계 비교 결과

앞의 사실을 같은 조건의 동종업계 회사·대체재와 비교해 객관적으로 확인되는 차이와 한계를 최대 3개 제시한다. 결과 우위까지 같은 조건으로 확인된 항목에만 `경쟁우위` 표현을 쓴다.

공통 열은 다음과 같다.

`차별점 | 비교대상·선정 이유 | 동일 비교축·기간·범위 | 우리 회사 근거 | 비교대상 근거 | 사업상 의미 | 비교 한계`

- 양쪽 자료가 같은 정의·기간·범위를 사용해야 하며 양쪽 출처를 모두 연결한다.
- 비교사 후보 선정은 자사가 발행한 DART 공시 또는 DART 기업개황의 정확한 HTTPS host에 결속된 자사 공식 HTML IR·공식 웹 원문만 사용한다. `corporate/about/product` 같은 안정 회사 페이지는 별도 문서일이 없으면 봉인된 최신 수집일을 현재성 날짜로 쓸 수 있다. 반면 host·path·query에 `news/newsroom/press/media/notice/release/archive/blog` 또는 문서형·검색형 경로/키가 있으면 봉인된 machine-readable 문서일이 필수다. 검색 결과 URL은 문서일이 있어도 항상 제외한다.
- 공식 웹 현재성은 개별 사실의 `as_of`가 아니라 `Report.as_of_date`를 기준으로 판단한다. 명시 문서일 또는 허용된 안정 페이지의 수집일이 잘못됐거나 미래이거나 보고서 기준일보다 400일을 초과하면 후보와 1~8장 canonical 직접 사실에서 제외한다. 캐시도 조회 당일을 새 기준일로 같은 검사를 다시 하며, 오래된 공식 웹이 하나라도 있으면 cache miss로 새로 조사한다.
- 공식 IR PDF는 제한 수집·일반 인용할 수 있지만 현재 수집 경로가 신뢰 가능한 문서 발행일·보고기간을 provenance에 결속하지 않으므로 raw preflight, v2 후보, 1~8장 canonical 선택·직접 사실에서 제외한다. PDF 수집일을 문서의 발행일·사실 기준일로 대신 쓰지 않는다.
- 후보 근거는 실제 1~8장 검증 사실을 역참조하는 v1과, 1~8장 의미 분류와 독립된 자사 공식 원문 한 문장·Source identity·위치·이중 해시·seal·attester를 잠그는 v2를 함께 읽는다. v1은 저장 JSON/HMAC 형식의 읽기 호환일 뿐이며 재출고할 때 현재의 엄격한 경쟁 관계 문법으로 다시 검증하므로 과거 v1도 `GATE_STOPPED`될 수 있다. 순수 경쟁 문장을 1~8장 `customer_market` 등으로 위장하지 않는다.
- v2의 `evidence_sha256`은 공백 정규화+casefold 문장, `evidence_exact_sha256`은 문장 추출 뒤 `strip`된 저장 문장 문자열 그대로의 UTF-8 SHA-256이다. exact 값은 원 HTTP/PDF 바이트 해시가 아니다. 두 값은 각각 `Source.evidence_hashes`와 `Source.exact_evidence_hashes`에 저장되고 두 목록 모두 Source HMAC seal에 결속한다. 일반 `FactRecord.state_evidence` 계약은 바꾸지 않는다.
- 경쟁 관계만 직접 적힌 문장은 관계만 증명한다. 고객·제품/서비스·시장 공통 범위는 양사의 별도 공식 원문으로 증명하고, 실제 재무 수치는 양사 DART의 같은 완료 사업연도·같은 회계 범위로 비교한다.
- v2는 `시장점유율`을 경쟁 표지로 인정하지 않고, 현재형의 명시적 경쟁 관계 문법에서 후보 법인명이 실제 target/list slot에 있을 때만 허용한다. 과거·former·potential, 부정·제외, 인용·제3자 귀속, 협력·지원, 고객·공급사·파트너의 경쟁, 모호한 별칭, `or/또는` 선택지는 거부한다. 이 닫힌 문법은 오탐을 막기 위해 정상 표현도 놓칠 수 있다.
- 후보용 웹·HTML IR은 HTTPS exact host만 허용하며 HTTP·인증서 fallback·cross-host redirect를 후보 근거로 쓰지 않는다. 공식 IR PDF 수집기는 robots.txt, HTML 5쪽·링크 60개·PDF 2개, 파일당 12 MiB·80쪽·추출 12,000자·160조각, 사이트 전체 PDF 20 MiB·보존 12,000자 상한을 적용한다. `pypdf 6.16.1`을 별도 프로세스에서 wall 10초·CPU 8초·메모리/주소공간 256 MiB·압축 해제 스트림 12 MiB·동시 워커 1로 제한하고, 앞 2쪽의 법인명·별칭을 확인한다. OCR 없이 글자를 추출할 수 없거나 production에서 OS 자원 상한 설정이 실패하면 거부한다.
- 구조가 다르기만 하면 `운영 특성`, 성과 우위까지 확인될 때만 `경쟁우위`라고 쓴다.
- 기술력·브랜드·네트워크·팬덤·인재 같은 일반명사는 결과 근거 없이 사용하지 않는다.
- 검증 가능한 차이가 3개보다 적으면 1~2개만 쓴다. 근거가 얇아도 빈 9장이나 추정 비교를 만들지 않으며, 최소 계약에 못 미치면 그 장을 한 번만 보충하고 결과를 다시 검사한다. 보충 뒤에도 미달이면 9장을 뺀 보고서를 내는 대신 전체 출고를 멈추고 비용도 청구하지 않는다.
- 단순 매출·자산 규모를 여러 계정으로 반복하거나 규모 자체를 우위로 승격하지 않는다. 같은 범위의 매출액과 영업이익으로 재현되는 영업이익률처럼 사업 해석에 유효한 축을 우선한다.

### 부록. 출처와 검증 상태

각 주장에 사용한 문서명·발행일·페이지와 공식·외부 자료 구분 및 검증 결과를 기록한다.

- 작성 과정, AI 사용, 레퍼런스 조사, 내부 검증 절차 설명은 본문에 쓰지 않는다.
- `잠정·예정`, 회계 재분류, 일회성 손익처럼 사실 해석에 필요한 한계는 해당 주장 옆에 남긴다.
- 사실의 발생 상태 `fact_status(actual/provisional/planned/estimated/scope_undisclosed)`와 원문 대조 결과 `verification_status(verified/partial/insufficient)`를 별도 필드로 관리한다.
- `state_evidence` 해시는 연결한 `Source.evidence_hashes`에 포함돼야 하고, 수치는 `numeric_checks`와 `ROUND_HALF_UP`으로 재현돼야 한다.

## 사실 단일 소유 원칙

- 모든 사실은 `회사·대상 + 지표 또는 사건 + 기간·시점 + 값 또는 상태`로 식별하고 본문 한 섹션만 소유한다.
- 같은 사실을 문장·표·그래프·카드 중 두 형식 이상으로 반복하지 않는다.
- 다른 섹션에서는 수치·날짜·사례를 다시 쓰지 않고 `결론 + 관련 장`만 표시한다.
- 핵심 요약은 Writer 검수를 통과한 본문 문장 3~5개만 글자 변경 없이 재사용하며 두 번째 실적표를 새로 만들지 않는다. 같은 장에서는 숫자 없는 문장을 우선하지만 검증 문장이 숫자를 포함한 것뿐이면 그대로 허용한다.
- 초안 완성 후 같은 수치·고유명사·사건의 반복 횟수를 검사하고, 필수 참조가 아닌 중복은 삭제한다.

### 출력 전 중복 검사 게이트

- `회사·대상 + 지표 또는 사건 + 기간·시점 + 값 또는 상태`가 같은 사실은 본문 전체에서 한 번만 허용한다.
- 같은 사실이 두 번 발견되면 PDF 출력을 중단하고 한 곳만 남긴다.
- 다른 장에서 필요할 때는 값을 복사하지 않고 `결론 + 관련 장`으로 연결한다.
- 단순한 핵심 용어 반복은 허용하지만, 같은 수치·날짜·실행 사례·파트너 역할의 재서술은 중복으로 본다.

### 섹션별 사실 소유권

- `2장`: 수익 구조, 고객 유형, 고객·지역·채널 우선순위
- `3장`: 제품·서비스별 근거와 핵심 사업 포트폴리오의 역할
- `4장`: 전사 3개년 실적과 완료된 변화
- `5장`: 아직 해결 중인 최신 문제와 현재 대응 수치
- `6장`: 미실행 목표·계획·일정
- `7장`: 운영 기반, 파트너 역할, 계약 범위
- `8장`: 공식 가치와 출처가 확인된 제한적 업무 사례. 직무별 맞춤 정보는 소유하지 않음
- `9장`: 동종업계 안에서 확인되는 자사 강점과 그 판단의 한계

## 시간 상태 중복 방지

- `#과거`: 최근 3년 동안 이미 실행됐고 변화가 확인된 사실만 다룬다.
- `#현재`: 아직 해결되지 않았으며 현재 대응 중인 과제만 다룬다.
- `#미래`: 아직 실행되지 않은 공식 계획과 목표만 다룬다.
- `사업 운영과 파트너 구조`는 확인된 기반과 관계를 설명하고, 경쟁우위 평가는 조건부 `동종업계 비교 결과`에서만 한다.
- `동종업계 비교 결과`는 자사 사실을 1~8번에서만 가져오고, 같은 조건의 비교를 위해 필요한 비교사 공식 자료만 9번에서 추가할 수 있다.
- 단, 순수 경쟁 관계를 1~8장 의미 사실로 위장하지 않기 위해 v2 독립 후보 원장은 9장 조립 전용 내부 근거로 둘 수 있다. DART 기업개황 `attestation_only` Source는 내부 Report JSON·저장·감사 등록부에만 보존한다. 공개 웹·PDF·Notion·Markdown 부록에는 후보 원문 citation만 내보내고 attester는 절대 표시하지 않는다.

## 본문 금지 문구와 정보

- `정리했다`, `읽는다`, `담았다`, `제시한다`처럼 작성 과정을 설명하는 문장
- AI 작성, 50개 레퍼런스 조사, 교차검증 절차 등 독자에게 필요하지 않은 제작 메타정보
- 출처가 약한 포괄적 지원자 행동과 회사명만 바꾸면 성립하는 일반론
- 직무별 업무·KPI·자기소개서·면접 답변 등 개인·공고별 맞춤 정보
- 동일 실적 수치·동일 사건·동일 파트너 역할의 반복

## 공통 출력 문장 틀

이 문장 틀은 생성 규칙으로만 사용하고 완성 보고서에는 회사별 사실을 채운 결과만 출력한다.

- 고객·시장: `[고객]은 [제품·서비스]의 [구매자·사용자·수혜자]이며, 회사는 [근거]에 따라 [시장]을 [핵심·성장·진입] 시장으로 둔다. 지역 전용 운영체계가 확인되면 [현지화]를 병기한다.`
- 포트폴리오 역할: `[핵심 자산]은 [고객·시장]에서 [수익 경로]를 만들며 전체 포트폴리오에서 [신규·성장·주력·안정] 역할을 맡는다. [불확실성]은 남아 있다.`
- 경쟁사 비교: `[회사]는 [비교대상]과 같은 [기간·범위]의 [비교축]에서 [객관적 차이]를 보인다. 양사 자료로 직접 확인되는 의미는 [출처가 설명한 범위]까지이며, 인과 근거가 없으면 관찰된 차이와 [비교 한계]만 적는다.`

## 런타임 적용

- 신규 출력은 `Report.schema_version=company-report-v4-canonical`을 사용하고, 캐시는 `product:company-analysis`과 `schema:company-report-v4-canonical` 토큰을 함께 사용한다. v3 이하 저장본은 v4 캐시 적중으로 인정하지 않는다.
- 캐시 적중 시에도 공식 웹의 현재성을 조회 당일 기준으로 다시 계산한다. 안정 페이지의 봉인 수집일 또는 문서형 페이지의 봉인 문서일이 조회일보다 미래이거나 400일을 초과하면 기존 보고서는 적중시키지 않는다.
- 모든 후보 사실은 `fact_id`, 정확한 대상, 기준시점, 시간 상태, `fact_status`, `verification_status`, 출처 범위·근거 해시, 소유 섹션과 수치 검산값을 가진 원장으로 관리한다.
- 9장 후보 수집의 실제 `state`는 `failed·none·ok`뿐이다. 정상 완전 탐색의 `none`만 후보 없음의 음성 증거다. 접속·TLS·MIME·파서·카탈로그 실패는 `failed`, 페이지·문서·바이트·글자 상한 잘림은 별도 상태값이 아니라 `candidate_scope_complete=False`와 상세 사유로 기록한다. 불완전한 `failed`나 `ok`는 `unknown`이며 후보 없음으로 바꾸지 않는다.
- 사실 선택 AI를 부르기 전에 공식 공시·IR·홈페이지 후보와 연속 3개 완료 회계연도 실적표가 있는지 검사한다. 둘 중 하나라도 없으면 선택 AI 호출 없이 중단한다. 입력이 갖춰지면 최대 2회만 호출하며, 1차가 필수 1~8장의 `claim_type`·내부 SID 링크를 완결하면 즉시 확정하고 불완전할 때만 2차를 호출한다. Writer에는 장별 상한 안에서 모든 SID 참조가 함께 들어 있는 완결 부분집합만 전달하며, 두 불완전 결과를 합치거나 세 번째 호출·3회 다수결로 보정하지 않는다.
- 작가 AI는 한 문장마다 하나의 원문 근거만 사용한다. 문장이 원문과 앞뒤 공백 제외 완전일치할 때만 코드가 확정해 Reviewer 호출을 생략하며, 공백 정규화·부분 일치·의미 유사성은 이 예외로 인정하지 않는다. 나머지 새 문장은 별도·무문맥 Reviewer가 원문과 대조하고, 명시적으로 거짓 판정된 문장만 한 번 재작성한다. 재작성문도 원문과 완전일치하면 코드가 확정하고, 나머지만 새로운 Reviewer가 다시 검사한다. 판정 누락·형식 파손과 두 번째 실패는 재시도하지 않고 삭제한다.
- 핵심 요약은 새 사실을 만들지 않으며, 위 Writer 검수를 완료한 본문 `FactRecord` 문장을 결정론적으로 골라 실제 본문의 `fact_ids` 부분집합과 결속한다. 요약의 검증 상태는 `verified_fact_reuse`이며 요약 전용 AI·Reviewer 호출 수는 0이다.
- canonical 런타임은 공개 사실이나 교차검증에 실제 쓰이지 않는 뉴스 검색·AI 선별을 생략한다. 공개 보고서에는 공식 공시·IR·회사 공식 홈페이지 근거만 사용하며 `newspick` 기능 파일은 기존 호출 호환용으로만 남긴다.
- 공개 웹·PDF·Notion은 같은 해시 결속 자동출고 정본을 렌더링하며 출력별 추가 질문이나 조언을 만들지 않는다. 필수 자동검사 전 항목이 통과하지 않으면 어느 채널에도 결과를 공개하지 않는다.
- 구형 DOCX 다운로드는 활성 출력이 아니며 PDF가 사용자 다운로드 정본이다.
- 자동출고 레코드는 같은 보고서·PDF·모든 페이지 PNG 해시와 전체 `fact_id`, 검사 버전을 결속한다. 사실·인용·수치·목차·금지 문구·PDF 렌더·채널 동등성 중 하나라도 실패하거나 검사 뒤 해시가 바뀌면 전체를 `GATE_STOPPED`한다.
- 파일럿 후보 원장은 P01~P25 25건이지만 현재 실제 유료 실행과 사용자 완전검수 범위는
  P01~P10이다. P11~P25는 이번 재시험 결과와 후속 범위 결정에 따라 평가한다. 이 검토는
  자동검사를 보정하는 파일럿 평가이며 서비스 건별 출고 승인 조건이 아니다. 기존 3인
  승인 DB 행은 감사자료로만 보존하고 출고 권한으로 읽지 않는다.

이 문서는 기존의 `1`, `2`, `3`, `4-1`, `4-2`, `4-3`, `9`, `참고`, `활용` 중심 출력 구조를 대체한다. 구형 키와 `job·requirements·guidance_lines`는 저장자료 호환 외에는 신규 보고서 내용이나 화면에 사용할 수 없다.
